# tapo/ 設定範本
#
# 用法：複製成同目錄的 .env，填入自己的值。
#   cp tapo/.env.example tapo/.env
#
# ⚠️ .env 本身**永遠不要進版控**（.gitignore 已經擋掉）。
#    但從 GitHub 網頁「Add files via upload」上傳會**繞過本機 .gitignore**——
#    "tapo 2.0/.env" 就是這樣進版控的（2026-08-27，公開曝露約 12 小時）。
#    請一律用 git commit 推檔案，不要用網頁上傳。

# 攝影機 RTSP 網址（內含帳密），格式：
#   rtsp://<帳號>:<密碼>@<相機IP>:554/stream1
# ⚠️ 程式**沒有預設值**：這一項沒填會直接中止並印出說明，不會安靜地跑下去。
CAMERA_RTSP_URL=rtsp://USERNAME:PASSWORD@192.168.x.x:554/stream1
