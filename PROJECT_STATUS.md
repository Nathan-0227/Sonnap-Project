# Sonnap 專案現況報告

> 更新日期：2026-08-25｜撰寫者：陳泰銘（AI 與數據處理）
>
> 這份文件的目的是讓團隊與指導老師一眼看出**哪些能動、哪些不能動、
> 哪些需要決策**。裡面點名的問題都附上可驗證的證據（檔案與行號），
> 不是印象或猜測。

> **2026-08-15 更新摘要**：確認專案目標是「可實際使用的產品、時間一學期以上」，
> 據此重新盤點的結論寫在第七節。一句話：**技術難的部分幾乎都完成了，
> 缺的是產品的部分**——整個系統目前假設世界上只有一個使用者，而且資料不會自己更新。
> 另外修掉一個會誤導的量測指標（見 6.7）與 AI 文案的重複問題（見 6.6）。

> **2026-08-17 更新摘要**：新增第八節，拿 GitHub 上一個同類型的開源專案
> （NightBloom）做逐行對照。兩個結論：**我們的評分設計禁得起對照**
> （對方 70% 的分數其實只反映單一訊號、睡眠時長權重是 0、五個門檻零文獻），
> 但**我們缺一整層「行為介入 → 回饋 → 成長」的迴圈**——對方有、我們沒有。
> 由此定下一條新的架構紅線（8.7）：遊戲化層只讀分數，不得回寫評分層。

> **2026-08-25 更新摘要**：**壓力與活動量的量測時窗都差了一天**，已修（見 6.8）——
> summary 的每一列混了兩個時段，睡眠歸「起床日」而步數/壓力照日曆日歸類，
> 結果兩者都落在那一覺「之後」。壓力那 11439 筆讀數裡**只有 8.6% 落在睡眠期間**，
> 與文獻文件定義的構念不符。修正後 Tier1/2 基礎分數 46 晚完全未變，
> 壓力修正的飽和率由 24% 回到 14%。
> 另外更正第 2.4 節：**睡眠分數其實有三套不是兩套**，漏掉的那套在
> `itegration/if_integrate.py`，用的是零文獻的寫死門檻。
> 新增 [PROPOSAL_GAP.md](PROPOSAL_GAP.md)：原始計畫書與現況的逐項對照，
> 每條主張都附行號可查證。

> **2026-08-28 更新摘要**：新增第九節，對照第二個外部專案 **Nyxo**
> （`hello-nyxo/nyxo-app`，301 stars，上架中的商業睡眠 App）。
> 🔴 **它是 GPL-3.0，一行都不能抄**——本節純粹是設計對照。
> 三個要點：**①** 它刻意**完全不計算睡眠分數**（只呈現時長與使用者主觀評分），
> 等於用迴避的方式繞開紅線 2，是報告「相關工作」很好的對照組；
> **②** 它的「就寢時段」用線性平均算時鐘時間，跨午夜會算出中午——
> 這條命中我們的挑戰系統，已實測驗過我們的 `bedtime_spread_minutes()`
> **站得住、不需修**（46 個窗格 0 個判定翻轉，見 9.5）；
> **③** 它的測試蓋滿了格式轉換、一條都沒蓋到會算錯的分析邏輯，
> 而兩個真正的計算錯誤全落在沒測試的那一欄——這正是
> `tests/test_scoring_guards.py` 存在的理由。

---

## 一、目前能動的東西

| 子系統 | 狀態 | 怎麼驗證 |
|---|---|---|
| Garmin 資料 pipeline | ✅ 完整可跑，約 2.3 秒 | `python garmin/run_pipeline.py` |
| 睡眠品質評分 | ✅ 51 晚實測（2026-05-29 ~ 08-23）**跨 3 名配戴者，見六、0** | `garmin/data/garmin_sleep_quality_final.csv` |
| App 資料檔 | ✅ 每次跑 pipeline 自動產生 | `app/assets/data/app_payload.json` |
| 後端 API | ✅ 回傳真實資料 | `uvicorn main:app` → `GET /get-sleep-data` |
| Flutter 首頁 | ✅ 顯示真實分數與寵物心情 | `cd app && flutter run` |
| AI 睡眠顧問 | ✅ 51 晚全數由 Claude API 生成 | `ai/data/ai_advice.json` |

### 資料流現況

```
Garmin 手錶
    ↓  garmin/run_pipeline.py（5 步驟）
garmin/data/garmin_sleep_quality_final.json
    ↓  build_app_payload.py（＋ ai/ 的建議，有才用）
app/assets/data/app_payload.json  ← 只有這一份檔案
    ├─→ Flutter 打包成 asset，App 直接讀
    └─→ main.py 對外服務 GET /get-sleep-data
```

**刻意只產一份檔案**：兩份就會出現「App 顯示的跟 API 回傳的對不上」這種
最難查的 bug。同一個檔案兩種讀法，結構上不可能不一致。

---

## 二、需要團隊決策的事（**這一節最重要**）

以下每一項都已經有可運作的預設值，但**預設值是我單方面決定的，需要 PM 核可**。

### 2.1 分數 → 寵物心情的映射（`mapping_version: 1`）

README 的 data contract 定義 `pet_mood` 只有四種合法值。目前的映射：

| 條件 | `pet_mood` |
|---|---|
| `final_quality == Good` | `happy` |
| `final_quality == Normal` | `bored` |
| `final_quality ∈ {Poor, Bad}` | `tired` |
| **覆寫**：壓力或心率明顯高於個人 baseline | `anxious` |

`energy_level = round(final_score)`。

51 晚實測分布：**happy 34、bored 9、tired 4、anxious 4**。

⚠️ 這組數字在 2026-08-28 大幅改變過（舊值 happy 26／anxious 10／bored 8／tired 2，
46 晚）。`anxious` 從 10 晚掉到 4 晚**不是調參數調出來的**——是修掉了
「拿別人的 baseline 比較」那個 bug（見六、0）。修正前有兩個 90 分以上的
Good 夜晚顯示焦慮寵物，那正是 `anxious` 與睡眠品質脫鉤的症狀。
定義在 `build_app_payload.py` 的 `QUALITY_TO_MOOD`，只有一個定義處。

**要決策的**：這個對應關係 UI/UX 組認不認同？`anxious` 用生理訊號觸發（而非分數）
是我的判斷，因為總分看不出「睡得夠久但自律神經緊繃」這種狀態。

### 2.2 `metrics.motion_count` 由誰提供

README 定義它是「翻身次數，由影像組提供之偵測數據」。
Garmin 也有一個 `movement_count`，但那是**我們自訂的翻動取樣筆數**，語意不同。

**目前 payload 裡 `motion_count` 是 `null`**，Garmin 的值另外放在
`garmin_movement_samples`。這是刻意的——硬對映會讓兩個不同的東西被當成同一個，
而且從此再也沒人發現。

**要決策的**：這個欄位最終由 TAPO 提供，還是改成用 Garmin 的值並改寫定義？

### 2.3 `metrics.ambient_noise_db` 由誰提供

**Garmin 手錶沒有這個數據。TAPO 目前的分貝是模擬值（見 3.2）。**
兩個來源都給不出真值，所以 payload 裡是 `null`。

**要決策的**：這個欄位要保留、移除，還是等到真的接上麥克風再說？

### 2.4 睡眠分數有**兩套 + 一個加權組合**，另有兩段死碼

> ⚠️ **這一節在 2026-08-25 被我改錯兩次才寫對，過程本身值得記下來。**
> 第一版寫「兩套」（漏了 `if_integrate.py`）；第二版改成「第三套是
> `calculate_garmin_score_from_features`，用 `stress > 20` 等寫死門檻」——
> 那條其實是死碼；第三版又寫成「`camera_score` 會從原始計數重算，
> 取代 TAPO 自己的分數」——**也是錯的**，SQL 裡早就 `sleep_quality_score
> as camera_score`，那段重算同樣是死碼。
>
> **兩次錯誤是同一個原因**：看到 `df['x'] = self.calculate_x(df)` 就假設它會執行，
> 沒往上看那個 `if 'x' in df.columns` 是否已經成立。這正是 8.2 定下的紀律
> （先讀實際呼叫路徑），而我對自己的專案連續兩次沒做到。

| 分數 | 位置 | 依據 | 狀態 |
|---|---|---|---|
| `final_score` | `garmin/apply_recovery_modifier.py` | 文獻加權（Hirshkowitz/Ohayon 等）+ 個人化修正 | ✅ 正式 |
| `sleep_quality_score` | `tapo/tapo_detector.py:326` | `large_turns×2.0 + micro×0.1 + snore×0.4`，扣分制 | ✅ 寫進 MySQL |
| `integrated_score` | `itegration/if_integrate.py:214` | `0.6×final_score + 0.4×sleep_quality_score` | ✅ **權重無依據** |
| `calculate_camera_score()` | `itegration/if_integrate.py:257` | `large_turn>20`／`events>50`／`snore>10` | ❌ 死碼——SQL 已 alias（`:130`） |
| `calculate_garmin_score_from_features()` | `itegration/if_integrate.py:225` | `stress>20`／`hours>11`／`deep<0.1` | ❌ 死碼——`final_score` 一定存在 |

✅ **重複檔已於 2026-08-26 處理完畢**：`itegration/if_integrate.py` 與
`tapo/if_integrate.py` 曾是逐位元組相同的重複檔（各 518 行，`diff` 零輸出），
使用者決定**保留 `itegration/` 那一份**，`tapo/if_integrate.py` 已刪除。
刪除前已驗證**全專案沒有任何程式 `import` 它們**（兩者都是獨立執行的腳本），
所以這次刪除不影響任何呼叫路徑。這是 `tapo/` 五支 detector 那件事的重演。

> ⚠️ 資料夾名稱 `itegration` 是 `integration` 的拼字錯誤。改名是獨立決定，
> 目前刻意不動——`requirements.txt:28` 的註解也指向這個名字，要改就一起改。

**⚠️ TAPO 分數量的是 timeline 長度，不是睡眠品質**（2026-08-30 實測，詳見 3.8 ⑤）。
扣分公式是事件數的線性函數，總扣分實測落在 2.9~905.5（差 312 倍），
而滿分只有 100 分可扣。決定性證據是**同一晚跨來源會差 80 分**
（08-02：SQL dump 80 分、per-night JSON 0 分），因為兩邊的 timeline
一個被清理過、一個是原始的。重跑：`python inspect_tapo_score.py`。

**要決策的**：App 顯示哪一個？整合的 60/40 從哪裡來？整合路徑本身的問題見 3.8。

### 2.5 `status.current_activity` 的即時狀態來源

contract 定義三種值（`sleeping`/`dreaming`/`waking_up`）用來驅動動畫。
但這需要「此刻」的狀態，而目前的 pipeline 是**隔日批次**產出的，給不出來。
payload 裡是 `null`，App 走 idle 動畫。

**要決策的**：要不要在 contract 裡加一個 `idle` 值？還是之後做即時串流？

### 2.6 contract 擴充欄位是否採納

為了讓 App 能運作，我在原 contract 頂層之外加了
`scoring` / `display` / `streak` / `history` 四個區塊，並在 `ai_content`
加了 `is_ai_generated` / `content_type` / `source`。

**沒有改動也沒有移除任何原有欄位。** 需要 PM 確認這個擴充可以寫回 README。

---

## 三、TAPO 側需要處理的問題

> 這一節點名的是隊友負責的部分。我用中性描述並附上可驗證的行號，
> **目的是讓問題被看見，不是評價任何人。** 這些都是很常見的開發中狀態。

### 3.1 時段判斷在真實睡眠時段永遠不會觸發

`tapo/motion_detector.py:50`：

```python
if now_str >= START_TIME and now_str < END_TIME and ...
```

這是**字串比較**。若把時間設成跨午夜的 `23:00:00` → `07:00:00`，
凌晨 01:30 的 `"01:30:00"` 不 `>= "23:00:00"`，條件**永遠是 false**。

目前設定是 `17:26:00`–`17:28:00`（第 12–13 行）的兩分鐘下午測試窗——
也就是說**它從來沒有真正監測過一整晚**。

修法：跨午夜時改成 `now_str >= START or now_str < END`。

**✅ 已於 2026-08-17 由影像組修好**（`tapo_detector.py:441-444`、`:689-692`）：

```python
if START_TIME < END_TIME:
    in_monitoring_time = START_TIME <= now_str < END_TIME
else:
    in_monitoring_time = now_str >= START_TIME or now_str < END_TIME
```

監測時段現為 `01:00:00`–`08:00:00`。**這一項可以結案。**

⚠️ 但要注意：`START_TIME` 仍是**排程時間**，不是「偵測到人進房」。
這代表攝影機**給不出「上床時刻」**，因此 3.5 提到的「入睡潛伏期」與
「真實睡眠效率」兩個文獻指標**仍然解鎖不了**。詳見 3.9。

### 3.2 分貝與打鼾是模擬值，不是量測值

`tapo/motion_detector.py:92, 94, 97` 用 `np.random` 產生 `sound_db`。
程式裡沒有麥克風、沒有音訊擷取。

這個模擬值往下流進 `snore_events`（第 195 行），再流進扣分公式
（第 199 行），最後影響它自己算出來的 `sleep_quality_score`。
**同一段影片跑兩次會得到不同分數。**

因為這樣，AI 顧問那邊已經明確擋掉這兩個欄位（`ai/README.md` 有說明）。

### 3.3 同一支程式曾有六份版本（已收斂，2026-08-15）

最多的時候有六份：`tapo/` 五支加上 `app/sleep_monitor.py` 一支，
**啟動時段各不相同，而且沒有任何說明哪支是正式的**：

| 檔案 | 監測時段 |
|---|---|
| `motion_detector.py` | 15:13–15:14（1 分鐘，測試設定） |
| `motion_detector2.py` | 22:00–23:00 |
| `motion_detector3.py` | 15:13–15:14（無資料庫版） |
| `motion_detector4.py` | 00:30–08:00 |
| **`tapo_detector.py`** | **00:30–08:00** ← 保留這支 |

已刪除其餘五支（commit `2d22565`、`71e18b7`），保留 `tapo_detector.py`：
它是最新上傳的版本、時段正確涵蓋睡眠時間，也是唯一有實際音訊擷取的版本。

⚠️ **請影像組確認**。全部都在 git 歷史裡，不同意 revert 那兩個 commit 即可。
已驗證刪除不影響任何程式（沒有任何 import 或執行路徑依賴它們）。

### 3.4 TAPO 已有 5 晚真實整晚資料（2026-08-15 更新，先前的判斷已過期）

⚠️ 這一節先前寫的是「內容是下午測試資料」。**對舊檔仍然正確，但對新資料
已經不成立**——影像組上傳了真實的整晚錄影，品質比先前好很多：

| | 舊的 `sleep_report.json` | 新的 `tapo/sleep_reports/` |
|---|---|---|
| 時段 | 15:47 起，僅 15 分鐘 | **01:07–10:29，真的整晚** |
| 整畫面誤判 | 普遍發生 | **6649 筆裡只有 1 筆（0.0%）** |
| 可用夜數 | 0 | **5 晚**（08-02～08-06） |

「整個畫面都在動」那個問題基本上解決了。其中 08-02、08-04、08-06
**與 Garmin 資料重疊**，可以做感測器交叉驗證——這是本專案第一次做得到。

**但有三個資料陷阱，接入時必須處理：**

1. **`report_date` 不可信**。程式寫的是「產生報告的時間」，不是那一夜。
   實例：標記為 08-04 的某份報告，內容其實是 08-03 那一夜
   （時間 01:41–06:50，影片檔名全是 `20260803`）。
   資料夾名稱同樣不可信。**日期要從影片檔名取。**
2. **4 份 `_recovered` 檔是壞的**，時間戳全是 `00:00:00`，且其中三份完全重複。
3. **`sleep_quality_score` 全是 0**（先前是撞地板值 50，現在直接掉到 0）。

### 3.7 新版程式的音訊分類是死碼（2026-08-15 新發現）

`tapo/tapo_detector.py` **真的做了收音**（用 ffmpeg 從串流抽音訊算 RMS），
這是進步——先前所有版本的分貝值都是亂數模擬的。

但單位對不上，可以直接算給你看：

```
db = 20 * log10(rms)     rms 來自 int16，最大 32767
→ 20 × log10(32767) = 90.3     所以 db 的理論最大值是 90.3

但門檻寫的是：
AUDIO_SILENCE_THRESHOLD = 200      ← 永遠達不到
AUDIO_SNOOZE_THRESHOLD  = 1500     ← 更達不到
```

**結果：`sound_type` 會永遠是 `"quiet"`，打鼾偵測整段是死碼。**
那兩個門檻看起來是給 RMS 用的，有人把變數從 RMS 換成 dB 卻忘了改門檻。

**第二個問題：這個 dB 不是聲壓級（SPL）。** `20·log₁₀(RMS)` 算的是數位訊號
振幅，會被麥克風靈敏度、前級增益、攝影機的自動增益控制、擺放距離影響，
而房間並沒有變吵。真正的分貝以 20 μPa 為參考基準，**必須用分貝計校準**。

→ 未校準的值**不能跨使用者比較**，也**不能在報告裡寫成「環境音 35 分貝」**。
它只在「同一次錄影、設定不變」時能反映相對變化（偵測打鼾事件是合理用途）。

**⚠️ 更新（2026-08-25）：影像組已於 8/17 嘗試修正，但問題仍然存在。**
門檻由 `200 / 1500` 改為 `100 / 800`，註解寫「Lowered for more sensitive detection」。
**但 `db` 的理論最大值是 90.3，而新的下限門檻是 100**——`db > 100` 仍然永遠是 false，
`sound_type` 仍然永遠是 `"quiet"`。改動只是把錯的數字調小，沒有處理根因：
**拿 dB 尺度的變數去比 RMS 尺度的門檻**。

舊值 `200 / 1500` 其實是**給 RMS 用的、而且很合理**（RMS 200 約為安靜房間、
1500 約為講話音量）。有人把變數從 RMS 換成 dB，門檻卻沒有一起換算。

換算對照（`20·log₁₀(RMS)`）：

| RMS | dB 值 |
|---|---|
| 200 | **46.0** |
| 1,500 | **63.5** |
| 32,767（int16 上限） | **90.3 ← 天花板** |

**修法兩種擇一**：(a) 直接比 RMS（`if rms > 1500`），回到原本的意圖，改動最小；
(b) 保留 dB，把門檻改成 `46.0` 與 `63.5`。
⚠️ 兩種修法都只解決「門檻不可達」，**不會讓這個數字變成聲壓級**——
上一段關於「不能寫成環境音 X 分貝」的限制依然成立。

**👤 分工（2026-08-26 決定）：這個 bug 交由影像組修，我不動 `tapo_detector.py`。**
理由跟 3.3（五支 detector 留哪一支）一致——那是他們的檔案，而且他們手上有
實際的收音環境可以驗證門檻。**交接時要一起講清楚的三件事**（缺一就會再修錯一次）：

1. 根因是**單位錯亂**，不是「靈敏度不夠」——8/17 那次註解寫
   「Lowered for more sensitive detection」，代表根因當時沒有被辨識出來
2. `db` 的天花板是 **90.3**，任何 ≥90.3 的門檻都是死碼（這是 int16 給的
   數學上限，跟房間多吵無關）
3. 修好之後**驗收方式**：跑一段真的有講話／打鼾的錄音，確認
   `sound_type` 出現過非 `"quiet"` 的值。**光看程式碼改了不算修好**——
   前一次就是改了但沒驗證

### 3.5 次要問題

- **RTSP 帳號密碼明碼寫死**在 `tapo/tapo_detector.py:18`，已進版控，
  而**本 repo 是公開的**。建議改成讀環境變數。
  ⚠️ 注意：刪掉程式碼裡的帳密**不等於移除**——git 會永久保留歷史。
  真正有效的動作是**修改攝影機密碼**。
- `sleep_records` 資料表**沒有任何建表 SQL**（只有 `INSERT INTO`），
  換一台電腦跑會直接失敗。
- `cv2.imshow` 讓它無法在無桌面環境（伺服器、排程）執行。
- **沒有偵測「上床時間」**——那是解鎖「入睡潛伏期」與「真實睡眠效率」
  這兩個文獻指標的關鍵，也是 TAPO 對本專案最大的潛在價值。
  ⚠️ 2026-08-15 查證：`START_TIME` 寫死為 `00:30:00`，**錄影是排程啟動而非
  偵測到人進房**，所以「第一個動作事件」只是「00:30 之後的第一次動」，
  不能當上床時間用。要解鎖仍需改成傍晚就開始錄 + 床鋪區域偵測。

### 3.6 ⚠️ 臥室影片會進版控的風險（已處理，但請注意）

兩支 TAPO 程式都會在自己的目錄下產生 `turn_YYYYmmdd_HHMMSS.mp4`
——**臥室錄影片段**。原本 `.gitignore` 沒有擋，只要有人跑一次再 `git add .`
就會上傳到 GitHub。

已在根目錄 `.gitignore` 加上 `turn_*.mp4`（遞迴套用到所有子資料夾）。
目前沒有影片被提交過。

### 3.8 ⚠️ 「Garmin + TAPO 整合」已經寫好了，但地基有五個問題（2026-08-25）

`itegration/if_integrate.py` 就是這件事的實作。核心是一行：

```python
df['integrated_score'] = (garmin_score * 0.6 + camera_score * 0.4).round()
```

**這個方向是對的，問題全部出在輸入端與參數。逐項列出，每一項都可獨立處理：**

**① 合併的鍵不可信（最嚴重）。** `pd.merge(camera_df, garmin_df, on='date', how='inner')`
用日期對齊，但 TAPO 側的 `report_date` 是**報告產生時間**而非那一夜（見 3.4，
實例：標記 08-04 的報告，內容其實是 08-03 那一夜）。**日期錯了，
就是把 A 晚的攝影機資料配上 B 晚的手錶資料**，而 `how='inner'` 會安靜地
把配不上的丟掉，不會有任何錯誤訊息。
→ **這一項不修，後面四項都沒有意義。** 日期必須從影片檔名取。

**② 可用樣本很少，但比先前記錄的多。**（2026-08-28 重新清點）

影像組 08-26 推上來的 `tapo/sleep_records.sql` 已經不是舊那份了：
**15 筆紀錄**（不是 5 筆），`sleep_quality_score` 有 **14 個不同的值**
（11 ~ 97）——先前記錄的「4 筆分數全是 50」那個症狀在新資料裡**不存在**。

> 🔁 **這一節的每個數字都可以重跑**：`python inspect_tapo_dump.py`
> （只用標準庫、不做任何評分）。影像組送新 dump 之後直接再跑一次，
> **不要照抄下面的表**——它是 2026-08-28 那份檔的結果。

重疊夜數要看你願意相信哪些列。用 `created_at` 對照 `report_date` 分層：

| 分層 | 筆數 | 判準 | 與 Garmin 51 晚的重疊 |
|---|---|---|---|
| 現場擷取（可信） | 9 | `created_at` 與 `report_date` 同日或差 ±1 天（跨午夜） | **5 晚**（08-17/18/21/22/23） |
| 事後批次補的 | 5 | `created_at` **全部是 `2026-08-12 13:21:29`**，同一秒 → 日期是人工填的 | 4 晚（08-02/04/06/07） |
| 幾乎確定是錯的 | 1 | id 48 宣稱 `2026-06-11`，卻在 **08-18** 才建立（差 68 天） | 1 晚（06-11） |

→ **最寬鬆的算法是 10 晚，最嚴謹的是 5 晚。** 報告要用哪個取決於
① 有沒有修好，也就是日期到底能不能信。

⚠️ 那筆 06-11 特別要小心：**它是唯一讓 TAPO 與 Garmin 六月資料產生重疊的紀錄**。
不做這個分層檢查就直接 `merge`，會憑空多出一個橫跨兩個月的「共同樣本」。

⚠️ **不論 5 晚或 10 晚，都不足以校準 60/40 的權重**（見 ④）。
樣本變多改變的是「能不能做交叉驗證的個案比較」，不是「能不能訂權重」。

**③ 扣分公式有一項恆為零。** `sleep_quality_score` 的
`snore_events × 0.4` 這一項永遠不會生效，因為打鼾偵測整段是死碼（見 3.7）。
舊版本更糟——分貝值直接由 `np.random` 產生（見 3.2）。

**④ 60/40 的權重沒有依據。** 沒有註解、沒有引用、沒有用資料校準過。
本專案 Tier1/2 的每一個權重都有文獻（`Garmin手錶分數.md` 附錄有對照表），
這裡是唯一的例外。

**⑤ TAPO 分數量的是「timeline 有多長」，不是睡眠品質。**（2026-08-30 重做，
先前的記錄不正確——重跑：`python inspect_tapo_score.py`）

公式是 `max(0, 100 − (large×2.0 + micro×0.1 + snore×0.4))`。
先前這裡寫「50 次大翻身就扣完 100 分」，**那只描述了一半的情況**，
會讓人以為調 `large_turn` 的係數就能修好。實測 24 筆紀錄：

| | |
|---|---|
| 總扣分範圍 | **2.9 ~ 905.5（差 312 倍）**，而滿分只有 100 分可扣 |
| 撞到 0 的紀錄裡誰主導 | micro_motion 5 筆、large_turn 4 筆（**沒有單一元兇**） |
| micro_motion 佔全部事件 | **93.0%**（23203 / 24949） |

係數不是問題，**尺度才是**：公式是事件數的線性函數，既不除以錄影時長、
也不隨偵測靈敏度調整。

**最強的證據是同一晚跨來源的落差**：

| 夜晚 | SQL dump | per-night JSON |
|---|---|---|
| 2026-08-02 | **80 分**（timeline 32 筆） | **0 分**（1177 筆） |
| 2026-08-04 | **51 分**（75 筆） | **0 分**（6649 筆） |
| 2026-08-06 | **49 分**（17 筆） | **0 分**（7428 筆） |

SQL 的 timeline 被 `clean_timeline_data()` 與 `MAX_TIMELINE_EVENTS` 削過，
JSON 是原始的。**同一晚可以是 80 也可以是 0，取決於你讀哪個檔**——
這比「恆為 0」嚴重，恆為 0 至少是誠實的無資訊。

→ 修法在影像組那一端：**`MOTION_MICRO` 偵測門檻**。一晚 7000+ 個
micro_motion 不是生理現象。（整畫面誤判是另一個獨立問題，見下方 ⑥。）

**⑥ `motion_intensity` 恆為 2073600 這個說法不成立。**（2026-08-30 實測更正）
24949 筆事件裡有 **20105 個相異值**，整畫面值 2073600 只有 **238 筆（0.95%）**，
而且全部集中在 `large_turn`（1017 筆中的 23.4%），micro_motion 一筆都沒有。
整畫面誤判是真的、值得修，但**修了不會改變分數的行為**。

**⑦ `snore_count` 是亂數的計數。** decibel 直方圖：35–41 dB 七個值
各佔 13.4–14.2%（最多與最少只差 1.052 倍），是 `np.random.randint` 的均勻分布；
34 dB 與 35 dB 之間還有斷崖。而現行程式根本不產生 `"snoring_or_noise"`
這個字串（`sleep_monitor.py:881-885` 只產生 quiet/medium/LOUD）
→ 資料是舊版程式寫的（3.3「六份版本」的殘留）。

✅ **等級標準衝突已修（2026-08-30）**：`if_integrate.py` 的 `SCORE_GRADES`
原本是五級（優秀/良好/普通/需改善/待加強，切點 85/70/55/40），與正式評分器的
四級（Good/Normal/Poor/Bad，切點 80/65/50）不一致——同一個 0–100 的數字
會被兩套標準講成不同的等級。現已改成四級並對齊切點。

**建議的修復順序**：① → ⑤ → ② → ③ → ④。① 與 ⑤ 是資料正確性
（日期對不上、分數全歸零），沒有它們後面都是空談；④ 放最後，
因為只有累積足夠的重疊夜數之後，才有辦法用資料談權重
（在那之前任何數字都是猜的，那就該誠實標成暫定值）。

**⚠️ 但在做這些之前，先讀 3.10——那裡主張根本不該做加權平均。**

### 3.9 「臥床時間」為什麼還是拿不到（2026-08-25）

先釐清缺的是什麼。臨床睡眠效率的定義是：

```
睡眠效率 = 總睡眠時間 ÷ 臥床時間(TIB)
TIB = 上床時刻 → 離床時刻
```

**Garmin 已經給了「入睡時刻」**，缺的是**「上床時刻」**——兩者之差就是入睡潛伏期，
也正是 6.3 記載的限制（目前分母用「起床 − 入睡」，實為「睡眠期間效率」）。

**攝影機是不是能提供上床時刻？**（2026-08-28 用現行 dump 重做，樣本從 3 晚增為 9 晚）

⚠️ 先前這裡的三晚表格取自**舊 dump**，其中 08-06 的「首事件 01:33」在現行檔
已經不成立（那一列的 timeline 時間戳全部是 `00:00:00`）。以下用現行檔重算，
可用 `python inspect_tapo_dump.py` 重跑。

| 日期 | 可信度 | TAPO 首事件 | Garmin 入睡 | 首事件 − 入睡 |
|---|---|---|---|---|
| 2026-08-17 | 現場擷取 | 01:53 | 02:24 | **−31 分** ✅ |
| 2026-08-22 | 現場擷取 | 06:04 | 06:41 | **−37 分** ✅ |
| 2026-08-02 | 事後補 | 01:25 | 02:13 | **−48 分** ✅ |
| 2026-08-04 | 事後補 | 01:07 | 01:40 | **−33 分** ✅ |
| 2026-08-18 | 現場擷取 | `00:00:00` | 04:56 | 時間戳壞掉 ❌ |
| 2026-08-06 | 事後補 | `00:00:00` | 03:22 | 時間戳壞掉 ❌ |
| 2026-08-07 | 事後補 | `00:00:00` | 03:59 | 時間戳壞掉 ❌ |
| 2026-08-21 | 現場擷取 | 03:30 | **22:31** | +299 分 ❌ |
| 2026-08-23 | 現場擷取 | 05:15 | **22:32** | +403 分 ❌ |

**方向是對的**：四個成功的夜晚，首事件都早於入睡 **31–48 分鐘**，
落在生理上合理的入睡潛伏期範圍，而且**四晚彼此一致**（全距只有 17 分鐘）。

但九晚裡有五晚失敗，而且失敗分成**兩種完全不同的原因**，要分開處理：

**失敗類型一：timeline 時間戳壞掉（3 晚）—— ✅ 已自行解決（2026-08-30）。**
整串事件的 `time` 全是 `00:00:00`~`00:00:04`。這是工程 bug，
但**不必等影像組修**：`video_clip` 的檔名是 `turn_YYYYMMDD_HHMMSS_*.mp4`，
**日期與時間都在檔名裡**，而且是錄影當下由檔案系統寫的，不經過那層會出錯的邏輯。

| 夜晚 | `time` 欄位說 | 從檔名還原 |
|---|---|---|
| 2026-08-06 | `00:00:00`~`00:00:04` | 01:06:09 → 06:04:34（17 筆） |
| 2026-08-07 | `00:00:00`~`00:00:04` | 02:21:07 → 11:27:37（42 筆） |
| 2026-08-18 | `00:00:00`~`00:00:01` | 00:51:53 → 次日 03:10:38（34/46 筆） |

實作在 `tapo_index.py`，回歸測試在 `tests/test_tapo_index.py`【2】。
**同一個機制也解掉了 `report_date` 不可信的問題**（3.8 ①）——那同樣改用檔名定日期。

（同類的第二個 bug，不在重疊夜裡所以不影響上表，但一起回報：
id 117／08-19 的 `total_events = 73`，`timeline` 卻是空陣列 `[]`——
**寫入端與統計端不同步**。任何以 `total_events` 為準的分析
（含 `sleep_quality_score` 的扣分公式）會落在一份不存在的 timeline 上。）

**失敗類型二：攝影機的監測窗看不到那個上床時刻（2 晚）。**
`tapo 2.0/.env` 設定 `SLEEP_START=01:00:00`，攝影機**凌晨一點才開機**。
08-21 與 08-23 的入睡時刻是 22:31 與 22:32——人早就睡著了，機器還沒開。
首事件 03:30／05:15 記錄的是「開機之後的第一次翻身」，不是上床。

> ⚠️ **這一項可以量化**：51 個 Garmin 夜晚裡有 **7 晚（14%）入睡在 01:00 之前**，
> 攝影機結構上拿不到它們的上床時刻。而觀察到的兩次窗口失敗
> （08-21、08-23）**兩晚都正好落在那 7 晚裡**——這證實了機制，不是巧合。

這也把本節原本那句佐證推到更強的位置：先前只能說「首事件都緊接在
01:00 開機之後，與『開機後的第一次動作』一致」。現在有兩個夜晚，
手錶明確指出人在 22:31 就睡著了，而攝影機的首事件在 03:30——
**首事件反映的是排程，不是行為。**

**所以核心結論不變，但理由更精確了：**

> **攝影機給的是一串動作事件，但沒有告訴你哪一個是「上床」。**

要算 TIB，得從中指認「這一個是他爬上床的時刻」，而現在每個事件長得都一樣
（只是「畫面有動」）。第一個事件可能是爬上床、可能是已睡著的人翻身、
可能是起床上廁所、也可能是窗簾動了。**這是辨識問題，不是資料量問題。**
（現行 dump 最多的一晚是 08-25 的 754 個事件。）

⚠️ **但 14% 那一項是純設定問題，值得先修**：把 `SLEEP_START` 從 01:00 提早
到傍晚，成本只是多錄幾小時，卻能讓那 7 晚從「結構上不可能」變成
「至少有機會」。這不能單獨解決辨識問題，但它是 (A) 的必要前置——
**演算法再好也認不出沒錄到的畫面。**

**三種解法，各有代價：**

| 做法 | 客觀性 | 工程量 | 適用對象 |
|---|---|---|---|
| **(A1)** 只把 `SLEEP_START` 提早 | — | **極小**（改一行設定） | 研究者。**先做這個**：見上方 14% 那一項，它是 A2 的前置 |
| **(A2)** 床鋪 ROI 佔用偵測 | 高 | **大**（要換演算法） | 只有研究者（1 台攝影機）|
| **(B)** App 加「我要睡了」按鈕 | 中（自我回報）| **小**（一顆按鈕）| **所有 D2 受測者** |
| (C) 兩者都做，用 A 驗證 B | — | A+B | 研究者 |

**(A2) 為什麼工程量大**：現在用的是**影格差分**（偵測「有沒有變化」），
而判定上床需要的是**區域佔用偵測**（偵測「床上有沒有人」）。
已經睡著的人不會產生變化，但他仍然「在床上」——這兩件事現行演算法分不出來。
通常要改成背景相減（先存一張空床參考影格）或在 ROI 內做人形／大物件偵測。

**建議 (A1) 立刻做（改一行設定），(B) 優先，(C) 作為報告素材。** 理由：**D2 的受測者不會有攝影機**，
(B) 是唯一能讓所有人都算得出 TIB 的做法，而且它剛好接在
`behavior/` 那一層既有的 `lights_out_at` 概念上。
(C) 則能產出「自我回報 vs 影像量測的偏差有多大」這個 N=1 的方法學結果，
比單獨做 (A) 更有寫進報告的價值。

### 3.10 建議：不要把兩個分數加權平均

**攝影機的獨特價值不是「對睡眠品質提供第二意見」，而是提供手錶量不到的測量值。**

把它當第二意見 → 必須 justify 60/40，而且是拿一個**每項都有引文**的分數
去平均一個**零文獻門檻**的分數，等於稀釋本專案最難複製的資產。

把它當缺失測量值 → **一個新參數都不用訂**：

```
現在:  integrated = 0.6 × final_score + 0.4 × sleep_quality_score
                     ↑ 每項有引文        ↑ 零文獻，且幾乎恆為 0

建議:  攝影機（或 App 按鈕）提供上床時刻 → TIB
         ├─→ 餵進 Tier1/2 既有的「睡眠效率」(25 分)，
         │    使其由「睡眠期間效率」升級為臨床定義（解掉 6.3 的限制）
         └─→ 解鎖 K 節「入睡潛伏期」（文獻已有門檻：Boulos 2019 + DSM-5）
       → 仍然只有一個分數，而且每一項仍然有引文
```

**若報告仍需要「整合」的說法**，誠實的做法是**兩個分數並列 ＋ 一致性指標**，
而不是加權平均。可用的指標由簡到嚴：

1. **等級一致率**——兩邊給同一晚的評級一不一樣（前提：先統一評級標準，見 2.4）
2. **Spearman 等級相關**——「當 Garmin 說 A 晚比 B 晚好時，攝影機同不同意？」
   只看排序不看絕對值，**完全不受尺度差影響**，正好繞開 3.8 的 ⑤
3. **Bland–Altman 分析**——醫學研究比較「兩種測量方法」的標準做法
   （Bland & Altman, 1986），可看出系統性偏差與一致性界限，有文獻可引

⚠️ **三種現在都跑不了**：Spearman 至少需 8–10 個點，Bland–Altman 建議 30+，
而目前重疊只有 3 晚。現行的 `score_difference`（逐晚絕對差）在 TAPO 分數
幾乎恆為 0 的情況下，測到的是尺度差而非共識，**沒有解讀價值**。

→ 順序：**先累積重疊夜數 ＋ 修好分數飽和（3.8 ⑤）→ 才談一致性。**

---

## 四、App 側現況

`app/` 是完整的 Flutter 專案（`lib/` 底下 24 支 dart 檔，5 個分頁）。

### 已接上真實資料

- **首頁**：睡眠分數、寵物心情、連續天數、所有文案都來自 `app_payload.json`
- 讀不到資料時顯示明確的錯誤狀態與解法，不是白畫面
- 夢境日記按鈕：有 AI 內容時顯示，並標明是**寵物的夢**與免責聲明

### ⚠️ 我多動了三個 widget 檔（原本計劃只碰 `home_screen.dart`）

接上真實資料後，`flutter test` 抓到三個 **RenderFlex overflow**（畫面會出現
黃黑斜線）。我用「隊友原本寫死的值」跑基準測試分離出責任歸屬：

| widget | overflow | 原因 | 誰造成的 |
|---|---|---|---|
| `header_card.dart` | 9 px | 倒數字串 `timeLeft` 在 30px 字級下會換行，撐破 150×150 的圓環 | **既有問題**——用隊友自己的值也會發生 |
| `sleep_score_card.dart` | 15 px | `message` 超過約 10 字就換行 | 我的字串太長 |
| `pet_mood_card.dart` | 11 px | `mood` 是 `"Anxious"`（7 字）時放不下 | 我的資料 |

三張卡都是固定 `height: 150`。處理方式：

- **`header_card.dart`**：`timeLeft` 包一層 `FittedBox(scaleDown)`，長字串縮小
  而不是換行。⚠️ **這個 bug 是時間相依的**——倒數字串的長度隨當下時間變化，
  所以只在某些時段出現，這也是為什麼一直沒被發現。
- **`sleep_score_card.dart` / `pet_mood_card.dart`**：固定 `height: 150` 改成
  `BoxConstraints(minHeight: 150)`。短字維持原樣，只有放不下時才長高。
  `home_screen.dart` 用 `IntrinsicHeight` + `stretch` 讓兩張卡一起長、保持等高。

**三個檔案的建構子都沒有改動**，呼叫端完全不受影響。
`build_app_payload.py` 那邊也加了 `MAX_SCORE_MESSAGE_CHARS = 10` 的檢查，
字串太長會在產生 payload 時就報錯，而不是等到有人打開 App 才看到斜線。

> Jeremy：這三個改動如果你不同意，還原它們不會影響資料層——
> 但還原後 `flutter test` 會失敗，而且畫面在部分情況下會出現黃黑斜線。

### 尚未接上（待處理）

| 項目 | 狀況 |
|---|---|
| **Insights 頁**（`report_screen.dart`） | 34 KB，資料寫死在 `CustomPainter` 內部。趨勢圖的 30 個數值、週心情、Top Sleep Distractions 都是假的 |
| **Top Sleep Distractions** | 顯示 YouTube/Instagram 用了幾分鐘——**這個資料完全沒有來源**，需要手機用量 API |
| **Sleep Tracking Sources** | 寫死顯示三個來源都「Active」，實際上 Camera 與 Phone 都沒有真實資料 |
| Assistant 頁 | `_getMockAiResponse()` 是關鍵字比對的假回覆 |
| Friends / Closet / Rewards | 全部是 coming-soon |

`history` 欄位已經在 payload 裡帶了最近 30 晚，Insights 頁要接的時候
不需要改後端。

### 語言不一致

App UI 是英文，但 `scoring.recommendation` 是中文（規則式評分的原文，
刻意不翻譯以保持事實來源單一）。目前後端額外提供英文短句給 UI 用。
`intl` 套件已經在 `pubspec.yaml` 裡，之後要做 i18n 有基礎。

---

## 五、設計文件裡零實作的功能

以下在 Figma 或設計文件中存在，但目前**完全沒有實作**：

寵物成長、好友睡眠狀態、好友比較、成就徽章、Closet、Rewards。

⚠️ **~~所有社交功能都被同一件事擋住~~：~~它們需要 `user_id`，
但目前所有資料輸出只有 `date`，沒有任何多使用者支援。~~**

> ✅ **2026-08-26 更新：這個阻塞點已解除。** `db.py` 的七張表全部以
> `user_id` 為主鍵的一部分，`main.py` 新增的端點都吃 `user_id`。
> 驗收見 `tests/test_api.py`（兩個使用者拿到不同 `user_id`，
> 各自上傳後 `GET /home` 回傳不同內容）。
>
> **同一輪也做掉了「目標就寢時間會存了」與「任務系統」**：
> - 目標就寢時間 → `users.target_bedtime`，`PATCH /users/{id}` 可改，
>   且 `nightly_behavior` 存**當晚的快照**（改目標不會追溯性地改寫
>   歷史達成度）
> - 任務系統 → `behavior/challenges.py` 三種型別，難度已用 46 晚校準
>
> ⚠️ **但社交功能仍然被另一件事擋住，而且那件事不會消失**：
> 跨品牌的生理指標不可互相比較（Garmin 的 REM 20% ≠ Fitbit 的 REM 20%，
> 與 Czeisler 2026 的 SRI 問題完全同型）。所以
> **好友比較只能建在 Tier A 的行為指標上**，不能比睡眠分數或深睡比例。
> 這會改變 `Research-Background/社交功能設計.md` 功能三的設計。
> 完整說明見 `wearable/__init__.py`。

---

## 六、已知的資料品質限制（誠實揭露）

這些不是 bug，是必須在報告裡誠實標註的限制：

0. **🔴 那 51 晚不是同一個人的。**（2026-08-28 才發現，優先度最高）

   這支手錶在 2026-05-28 ~ 08-27 之間**經手三個人**，專題負責人本人 08-28
   才開始配戴。計分夜晚的分布是：

   | 區段 | 期間 | 計分夜數 | 可信 |
   |---|---|---|---|
   | `wearer_a` | 05-28 ~ 07-27 | **41** | ✅ 確定同一人 |
   | `unverified` | 07-28 ~ 08-27 | **10** | ❌ 已知多人戴過，訊號分不出是一人還兩人 |
   | `wearer_c`（負責人本人） | 08-28 起 | **0** | ✅ 但還沒有資料 |

   ⚠️ **報告不可以寫「51 晚單一使用者實測」。** 誠實的寫法是
   **「跨 3 名配戴者、51 晚」**。

   ⚠️ **這不會使分數失效，但會限制能宣稱什麼**：
   - **Tier1/2 基礎分數完全不受影響** —— 文獻加權、不依賴個人 baseline，
     所以每一晚的分數本身仍然站得住。
   - **Tier3 個人化修正值與 SRI 是個人內比較**，跨人算沒有意義。
     `unverified` 那 10 晚的 Tier3 與 SRI 已經全部關掉。
   - **「長期追蹤同一人的趨勢」這種敘述不能講** —— 我們沒有任何一個人
     連續 51 晚的資料，最長的是 `wearer_a` 的 41 晚。

   ⚠️ **負責人本人目前有 0 個計分夜晚**，而 `MIN_BASELINE_NIGHTS = 14`，
   所以就算從 08-28 起每晚都戴，**09-09 之前 Tier3 都還在冷啟動**。
   demo 當天他自己的資料只會有 Tier1/2。

   分界是用生理訊號本身找的（沒人記得交接日期）：A→B 分界處靜止心率跳
   5.73~6.74、睡眠期間平均心率跳 4.13，而前 41 晚內部三個訊號的最大跳躍
   都 ≤ 1.03。決定性的證據是 `avg_heart_rate` 量的是**睡眠期間**——
   配戴習慣會改變步數與白天讀數，但不會讓人睡著時的心率持續高 21 bpm
   連續 27 晚。實作見 `garmin/apply_recovery_modifier.py` 的 `WEARER_SEGMENTS`。

1. **手錶不支援 Garmin 官方睡眠分數**（Vivoactive 3 沒有 `sleepScores` 欄位），
   所以無法用官方分數驗證我們的評分。
2. **51 晚裡有 13 晚 REM = 0**，那是舊錶的偵測限制，**不代表沒有 REM 睡眠**。
   程式與 AI 都把這種情況標成「未測得」而不是 0。
3. **`sleep_efficiency` 不是臨床真效率**。因為缺「上床時間」，分母用的是
   （起床 − 入睡），實為「睡眠期間效率」，不含入睡潛伏期。
4. **配戴率偏低**：87 個日曆日裡只有 51 晚有記錄（約 59%），
   而且起床後會取下手錶，所以白天步數（中位數 153 步）無法反映真實活動量。
   活動量修正因此自動停用。這也讓 App 的「連續天數」數字偏小——那是誠實的結果。
5. **作息規律指數（SRI）刻意不計分**，只呈現數值。理由：不同計算方法算出的
   SRI 差異大到足以改變研究結論，拿去對照外部常模沒有意義。

6. **AI 文案是生成內容，不是量測值**（51 晚全數由 `claude-sonnet-5` 生成）。
   `ai_content.content_type` 標成 `fiction+advice`：`dream_summary` 是**寵物自己的夢**，
   **不是使用者的夢**——手錶完全沒有量測夢境內容，宣稱知道使用者夢到什麼就是誤述資料。

   數值面有四道確定性防線（全部在 `ai/generate_advice.py` 的 `validate()`，
   通不過就重試，兩次都不過才退回規則式文字）：
   - 夢境**不得出現任何數字**（正規表示式擋阿拉伯與全形數字）
   - 建議裡出現的每個數字**必須逐字存在於事實區塊**
   - 禁詞表擋掉醫療宣稱（診斷／治療／藥物…）
   - 「日記中間有幾頁是空白的」這類**「這段沒有記錄」的意象，只有 REM 真的
     沒測到的夜晚才准用**

   最後一條是這輪最值得記的教訓。2026-08-12 第一次真實呼叫，2026-08-09 那晚
   REM 明明測到 29 分鐘，模型仍寫了空白頁——**等於對使用者謊稱這段沒有資料**，
   跟本專案堅持三個誠實 `null` 是同一件事。根因是 few-shot 把「評級 Bad」與
   「REM 未測得」兩個獨立條件綁在同一個範例裡，模型在 Bad 的夜晚就照抄。
   已改成依當晚事實動態組裝 prompt，並加上驗證層——而驗證層真的救到了：
   修好 prompt 之後模型仍會再犯，是被擋下重試才過的。

   **2026-08-15 補充：夢境內容重複的問題已大幅改善，但未完全解決。**
   全量檢查發現 46 晚裡有 32 晚（70%）不是「圖書館」就是「海床」。
   根因不是模型偷懶——調色盤每個條件只給一組意象，又要求「只能用當晚事實
   支持的意象」，事實相似的夜晚必然寫出幾乎一樣的夢。已擴充調色盤並新增
   「避開最近 7 晚用過的意象」機制：

   | 指標 | 修正前 | 修正後 |
   |---|---|---|
   | 「圖書館 or 海床」 | 32 晚（70%） | **8 晚（17%）** |
   | 用到的意象種類 | 5 種 | **16 種** |
   | 退回規則式文字 | — | **0 晚**（46/46 全由 LLM 生成） |

   ⚠️ **仍未解**：集中點搬到「棉被與雪」（17 晚，37%）。診斷已完成——
   意象分類的粒度與調色盤不對稱（深睡類 6 個選項只對應 2 個分類，
   REM 類 6 個選項對應 5 個分類，所以只有深睡類出問題）。修法約 6 行。

7. **⚠️ 修掉一個會誤導人的量測指標（2026-08-15）。**
   原本輸出的 `movement_count` 被當成「翻動量」，但它**完全沒有測到動作**——
   程式對 Garmin 每筆睡眠動作記錄只做計數，把記錄裡的動作幅度數值整個丟掉，
   而那是每分鐘固定一筆的取樣，所以它等於「手錶錄了幾分鐘」。

   四條可重跑的證據：程式碼路徑（幅度從未被讀取）、取樣間隔 99.98% 恆為
   60 秒、與睡眠時長相關 r = +0.929 而與夜間清醒僅 −0.138、被丟掉的數值
   其實是 28497 筆範圍 0.00–7.64 的連續值。

   **不修的實際後果**：2026-08-09 那晚舊指標是 489，四晚裡最低，看起來像
   「動最少」；但真實訊號顯示動作幅度是其他夜的兩倍、活躍時間佔 72%。
   那正是評級 Bad 的一夜——**舊指標會給出相反的結論**。

   已改名為「取樣分鐘數」並補上三個真實動作指標。
   ⚠️ 這幾項**刻意不納入評分**：判定「有明顯動作」的門檻是看資料分布訂的，
   沒有文獻依據，而本專案每一項計分都有引文。
   已驗證修改前後 46 晚的分數**逐位元組完全相同**。

8. **⚠️ 壓力與活動量的量測時窗都差了一天（2026-08-25 已修）。**

   根因是同一件事：`garmin_sleep_summary.csv` 的每一列**混了兩個時段**。
   睡眠歸「起床日」（對齊 Garmin 官方的 `calendarDate`，使跨午夜的夜晚不被
   切成兩列），但步數與壓力的時間戳不落在睡眠區間內，因此照自己的日曆日歸類
   ——**兩者都發生在那一覺「之後」**。分流的那一行是
   `analyze_garmin_sleep.py:185`。

   **壓力（±4，Tier3 額度最大的一項）**：11,439 筆讀數中**僅 8.6% 落在睡眠期間，
   91.4% 是白天**，而文獻文件 G 節定義的構念是「睡眠期間的生理壓力負荷」。
   又因本資料集平均就寢 02:34，列 D 的傍晚讀數（19:00–23:00，配戴高峰）
   實際屬於**下一晚**的睡前時段——單一欄位橫跨了兩個不同夜晚。

   已新增 `presleep_stress_score`，定義為「**上一次起床 → 這一次入睡**」的整個
   清醒時段。窗格兩端皆由資料自身界定，**不含任何人為選定的時間長度**。

   | 46 晚實測 | 修正前 | 修正後 |
   |---|---|---|
   | 壓力修正生效 | 34/46 晚 | **14/46 晚** |
   | 飽和率 | 24% | **14%** |
   | 改變分級 | — | 3 晚 |
   | **Tier1/2 基礎分數** | — | **完全未變** |

   生效夜數下降是因為需要**連續兩晚都有記錄**（與 SRI 受同一結構限制，
   可用夜數同為 28）。**這不是永久限制——配戴率改善它就自己補起來；
   而舊定義的構念錯誤再收集 200 晚也還是錯的。**

   單一夜晚差異可達整個 ±4：2026-07-12（02:39 入睡、15:29 起床）舊值 25.8
   （基準 5.5）扣滿 −4.0，而那 25.8 **整段發生在起床之後**；新值 3.3
   （基準 5.4），入睡前的清醒時段實際上低於個人常態。同一批原始讀數差 7.8 倍。

   **活動量**：改讀前一個日曆日的 `steps_total`（H 節引的 Kredlow 2015 檢驗的是
   「日間活動 → 其後睡眠」的前瞻關係）。此項已因 baseline < 1000 步而停用，
   **數值零影響**。

   ⚠️ **本修正不宣稱解決了因果方向問題。** 文獻對「壓力→睡眠」與
   「睡眠→隔日壓力」何者較強結論分歧（本專案 G 節引的 Yap 2020 即證實雙向），
   而 46 晚資料也分不出來（三種時窗與基礎分數的相關係數 −0.189／−0.283／−0.258，
   兩兩差距遠小於 n=46 時約 0.155 的標準誤，且**均未達統計顯著**）。
   此修正處理的是**可查證的事實問題**——「這個數值量的是哪一段時間」。

   **順帶抓到一個會汙染 baseline 的問題**：2026-06-02 那筆已知異常列
   （`sleep_start == wake == 00:00:00`）雖然被 `extract_sleep_features.py` 濾掉，
   但 **`apply_recovery_modifier.py` 讀的是 `summary` 而不是過濾後的 features**，
   假值會直接進入 baseline 的歷史清單（實測會讓生效夜數多算一晚）。
   已在 `build_awake_windows()` 自己擋掉退化區間，不倚賴下游過濾。
   → **任何讀 `summary` 的新程式都得自己做有效性檢查。**

   **一個意外的旁證**：最新一晚（08-09）的 `pet_mood` 仍是 `anxious`，
   但觸發規則從 `stress_modifier` 換成了 `rhr+avg_hr modifier`——
   而後兩者都是在**睡眠期間**量的。原本的判斷並非只靠那個時窗錯誤的壓力值撐著。

---

## 七、建議的下一步

> **2026-08-15 重新排序。** 目標已確認為「可實際使用的產品，時間一學期以上」，
> 而不是期末 Demo。在這個標準下重新盤點，優先順序跟先前完全不同——
> 過去排「低」的多使用者支援，現在是**最高優先**。

### 盤點結論

**技術難的部分幾乎都完成了；缺的是產品的部分。**

睡眠評分有完整文獻依據、AI 有四道確定性驗證、資料鏈路已經打通。
但有兩個根本缺口，不解決的話它就是一支很精緻的個人分析腳本，不是產品：

**缺口一：整個系統假設世界上只有一個使用者**

| 位置 | 現況 |
|---|---|
| `garmin/.env` | 一組帳密 |
| `app_payload.json` | **一份**檔案，屬於誰不明 |
| `main.py` 的 API | 不接受任何參數，永遠回同一份 |
| `settings_screen.dart:26` | 使用者名稱寫死 |
| `settings_screen.dart:169` | UI 自己寫著「登出功能待驗證機制完成後接上」 |
| `friends_screen.dart` | 四個好友全部寫死 |

`Research-Background/社交功能設計.md` 裡四個有文獻依據的功能
（好友狀態、資訊可視化、平均比較、成就徽章）**全部依賴這件事**。
而且**越晚做越貴**——等 Insights / Friends 都寫完再回頭改，成本會高很多。

**缺口二：資料不會自己更新**

1. 抓取端**沒有排程也沒有增量**，每次都要手動指定日期並整段重抓
2. App 讀的是打包進去的檔案，**資料凍結在 build 當下**——
   使用者裝了 App 之後，看到的數字永遠不會變

Demo 沒問題，產品不行。好消息是資料層已經預留了改打 API 的位置，
換一個實作類別即可，UI 一行不用動。

**另外：五個頁面只有一頁接了真實資料**（Home ✅；Insights / Friends /
Assistants / Settings 仍是寫死或罐頭內容）。

### 排序（不可逆的架構決定先做）

> ⚠️ **2026-08-26：這張表的第一列已經不成立了，保留是為了記錄它怎麼消失的。**
>
> 原本寫「開全隊會議定架構：要不要做多使用者？帳號怎麼來？資料存哪？
> Garmin 帳密要每個使用者交出來，這是隱私問題不是技術問題」——
> 那三個問題**全部已經有答案，而且不是靠開會決定的，是被 D2 的定義決定的**：
>
> - **要不要做多使用者** → D2 確認「找同學裝 APK 實際用一到兩週」之後，
>   這就不是選項了。已實作（`db.py` + `main.py`）。
> - **帳號怎麼來** → 暱稱制免註冊，不收 email 與密碼。對受測者最友善，
>   隱私面也最乾淨。⚠️ 代價是 `user_id` 本身即憑證，同意書要寫明。
> - **資料存哪** → SQLite（標準庫 `sqlite3`，`requirements.txt` 不多任何一行）。
> - **Garmin 帳密的隱私問題** → **這個問題消失了。** 經費只有一支手環，
>   受測同學不會有錶，所以 Garmin 永遠只綁研究者本人。
>   其他人要客觀睡眠資料走 **Health Connect**（自備裝置，不交任何帳密）。

| 優先度 | 事項 | 負責 |
|---|---|---|
| **最高** | ~~開全隊會議定架構~~ ✅ **已解決，見上方說明** | ~~PM／全隊~~ |
| **最高** | **Android 端**：UsageStats（`lights_out_at` 與睡前 App 分布）、<br>Accessibility 阻斷、Health Connect 串接。<br>**後端已全部就緒**（端點與 schema 都在，`app_usage_daily` 與<br>`block_events` 兩張表已建好、目前是空的），缺的是手機側 | **前端／我** |
| 高 | 抓取加排程與增量；App 改成打 API 而非讀打包檔 | 我 |
| 高 | 決定 TAPO 五支程式留哪一支（3.3）；修時段邏輯（3.1） | 影像組 |
| 高 | 修音訊分類的門檻 bug（見 3.7）。**08-26 已確認由影像組負責**，<br>交接要點與驗收方式見 3.7 末段 | 影像組 |
| 中 | Insights 頁接上 `history` 資料（後端已備好，零改動） | 前端 |
| 中 | 移除明碼帳密、改讀環境變數（3.5） | 影像組 |
| 中 | TAPO 接進 payload + 三晚交叉驗證 | 我 |
| 中 | Settings 加上設定持久化 | 前端 |
| 中 | 人工抽查 51 晚的 AI 文案（已全數生成，見 6.6） | 我／PM |
| 中 | ~~**新增（08-17）**：行為挑戰層，標的用 SRI~~ ✅ **已完成（08-26）**，<br>但**標的改用就寢時間的收斂度，不是 SRI**——見 8.6 的修正 | 我／PM 核可 |
| 低 | Friends 頁。~~卡在多使用者，沒做就做不了~~ ✅ 多使用者已就緒，<br>⚠️ **但比較的指標只能用行為，不能用睡眠分數**（見第五節） | 前端 |
| 低 | 部署（容器化 + hosting） | 全隊討論 |

### 我這邊本週完成的（不依賴任何人）

- 刪除 `app/` 底下的 TAPO 程式複本；`tapo/` 的五支 detector 收斂成一支
- 修掉會誤導的量測指標（見 6.7），已驗證分數零變動
- AI 文案重複率 70% → 17%，數字格式統一（見 6.6）

### 2026-08-25 這一輪完成的

- **壓力與活動量的量測時窗修正**（見 6.8）。Tier1/2 基礎分數 46 晚完全未變
- **`db.py`：多使用者持久化層**。SQLite 用標準庫 `sqlite3`，
  `requirements.txt` 不多任何一行。暱稱制免註冊，不收 email 與密碼
- **`behavior/`：Tier A 行為層**。架構紅線 8.7 在新結構下**不需要靠紀律守住**——
  遊戲化建立在手機行為資料上，與評分器不在同一條路徑，可用 grep 機械化驗收
- **[PROPOSAL_GAP.md](PROPOSAL_GAP.md)**：原始計畫書與現況的逐項對照

### 2026-08-26 這一輪完成的（多使用者迴圈打通）

整輪的驗收都可重跑：`python tests/test_api.py`、
`python tests/test_healthconnect_adapter.py`。

- **`main.py` 多使用者端點**：`POST /users`、`PATCH /users/{id}`、
  `DELETE /users/{id}`、`POST /nightly`、`POST /wearable`、
  `GET /home`、`GET /insights`、`GET /challenges`。
  ⚠️ 舊端點 `/get-sleep-data` **行為完全不變**（有回歸測試）。
  CORS 原本只開 `GET`，所以**結構上就不可能寫入**，已補上 POST/PATCH/DELETE。
- **`wearable/healthconnect_adapter.py`**：Health Connect 的睡眠資料走
  **既有評分器**算 Tier1/2，一個門檻都沒改。手刻一晚驗證分數與手算相符（98.8）。
- **`migrate_garmin_to_db.py`**：51 晚灌進 `wearable_nightly`，
  與 `garmin_sleep_quality_final.csv` **逐列相符**，可重複執行。
- **`behavior/challenges.py` + `behavior/pet_state.py`**：挑戰引擎與行為驅動的寵物狀態。
- **挑戰難度用 46 晚校準過**（見下方）。

**⚠️ 三個值得記下來的發現：**

1. **臥床時間（3.9）對 L1 同學其實拿得到。** Health Connect 的
   `SleepSessionRecord` 以「**上床**」為 session 起點，所以
   TIB、入睡潛伏期、臨床睡眠效率全部算得出來——攝影機原本要解的問題，
   對有穿戴裝置的受測者來說標準層直接就給了，一個新參數都不用訂。
   ⚠️ **但刻意不拿它計分**：Garmin 的效率分母是「起床 − 入睡」，
   臨床定義是「起床 − 上床」，後者一定比較低。兩種數字餵進同一組門檻
   （`EFFICIENCY_GOOD = 85`），L1 同學會被**系統性地扣分**，
   而那個差異來自裝置不是來自睡眠。→ 另存 `clinical_efficiency` 供報告用。
   日後 3.9 若在 Garmin 側解決，兩邊要**同時**切換並重跑歷史資料。

2. **挑戰難度：原本的門檻有一個真的達不到，另一個是被假象騙的。**
   - `作息收斂` ±30 分鐘 → 36 個窗格 **0/36 = 0%**，完全達不到
     （該使用者 7 晚離散度中位數 103 分鐘）。已改為 **±60**（22%）。
   - `連續 5 晚` → 第一次跑出 0%，**一度誤判成挑戰設計壞了**。
     查下去發現有一部分是**手錶配戴率造成的假象**：51 晚散在 87 個日曆日、
     只有 1 段連續 ≥5 天，而「缺資料就中斷」的定義讓斷點吃掉了大部分連續。
     **那個限制在 Tier A 不存在（手機天天都在）**，所以校準時要先把斷點拿掉。
     去掉斷點後量到「準時率要到 55–60% 才連得到 5 晚」，已改為 **連續 3 晚**。
   - ⚠️ 校準樣本 **n = 1**，而且那個人特別不規律（就寢標準差 84 分鐘）。
     這些是暫定值，D2 拿到真實資料後要重跑
     （`python migrate_garmin_to_db.py --simulate-challenges`）。

3. **`users.target_bedtime` 的預設值 23:30 會讓夜貓子第一天就放棄。**
   「準時放下手機」的達成率**完全由使用者自己設的目標決定**：
   同一批資料，目標 23:30 → 2%、目標 02:30 → 43%、目標 03:00 → 59%。
   → **註冊流程必須問「你現在通常幾點睡」並據此建議目標**，
   不能給所有人同一個預設值。這是 App 端的事，已列入待辦。

### ⚠️ 2026-08-26 新增的待辦（給其他人）

| 對象 | 事項 |
|---|---|
| **Jeremy** | 註冊流程要問「你現在通常幾點睡」並據此建議目標就寢時間。<br>照預設值 23:30 開始用的夜貓子**第一天就會看到 0%**（實測 2%）。<br>後端已就緒：`POST /users` 收 `target_bedtime`、`PATCH /users/{id}` 可改 |
| **PM／全隊** | 社交功能的比較指標**只能用行為**（就寢達成率、熬夜比率），<br>不能比睡眠分數或深睡比例——跨品牌分期演算法不互通。<br>`社交功能設計.md` 功能三要改設計 |
| **PM** | 知情同意書要寫：收集 App 使用與睡眠生理資料、`user_id` 即憑證<br>（無帳密）、退出即刪除（`DELETE /users/{id}` 已實作並驗證 CASCADE） |

### ⚠️ 2026-08-25 新增的待辦（給其他人）

| 對象 | 事項 |
|---|---|
| **PM** | 三套評分要留哪些（見 2.4）。這是第一次發現有三套 |
| **影像組** | 請跑一次 `SHOW CREATE TABLE sleep_records` 給出現行結構。<br>`feature/opencv-motion-garmin` 分支上那份 `sleep_records.sql` **不完整**——<br>`tapo_detector.py:200` 寫入的欄位與它相符，但 `sleep_anylzer.py:50` 會 SELECT<br>`created_at`，而那份 CREATE TABLE 沒有這一欄，一跑就 `Unknown column` |
| **全隊** | `feature/opencv-motion-garmin` 的另外 11 個 commit **不能合**——<br>會刪掉 `app`、`backend`、`docs`，還會救回已刪除的 `garmin_importer.py` |
| **Jeremy** | `HeaderCard` 與 `SettingsScreen` 各持有獨立的 `targetBedtime` state，<br>在 Settings 改就寢時間首頁倒數不會變（`header_card.dart:32`、<br>`settings_screen.dart:53`、`main.dart:35-41`）。建議開 Issue |

---

## 八、外部開源專案對照（2026-08-17）

> **這一節的用途**：報告的「相關工作」章節可以直接引用，同時把我們一直
> 在遵守但從沒白紙黑字寫下的設計原則，用一個具體的反例固定下來。
>
> ⚠️ **先講分寸**：對照對象是一個 4 stars、34 commits 的小型學生專案。
> 「它有問題」**不能反過來證明我們就是對的**。放這一節是因為它的問題
> 剛好逐項對應到我們刻意做過的取捨，可以把抽象原則變成看得見的東西。

### 8.1 對照對象與查核方法

| | |
|---|---|
| 專案 | NightBloom（`shev0k/sleep_tracker`） |
| 規模 | 34 commits、4 stars、2 個 fork、6 條分支（全是已合併的 feature 分支） |
| 活躍度 | **最後一次 push：2024-11-02**，停更約 21 個月。⚠️ 判準見 8.1.1 |
| 授權 | **不明**。README 宣稱 MIT 並要讀者去看 LICENSE 檔，但 repo 裡**沒有那個檔案**，GitHub 偵測不到授權。見 8.8 |
| 技術棧 | Flutter + Node.js/Express + MongoDB |
| 定位 | 遊戲化睡眠追蹤：睡眠品質驅動一個會成長的虛擬花園 |

**查核方法：clone 下來讀原始碼，不採信 README 的自述。**
這一點是本節最重要的方法論結論，理由見 8.2。

### 8.1.1 ⚠️ 判斷「停更了沒」要看 `pushed_at`，不是 `updated_at`（2026-08-28 複查）

有人回報「NightBloom 好像更新了」，複查後**沒有**。差點下錯結論的原因是
GitHub 有兩個長得很像的時間欄位：

| 欄位 | 意思 | NightBloom 的值 |
|---|---|---|
| `pushed_at` | **最後一次 push 程式碼**（推到任何分支都算） | **2024-11-02T19:16:57Z** |
| `updated_at` | 最後一次 metadata 變動（**被 star、被 watch、改描述都會動**） | 2026-04-29T10:14:55Z |

兩者差 **18 個月**，而這中間一行程式碼都沒有進來。三條交叉驗證：

1. 全 repo 最新 commit 是 `Reward names fix`，2024-11-02T19:16:38Z
2. 6 條分支全部停在 2024-10～11（`backend-setup`、`daily-challenges-sleep-score`、
   `progress-feature` 都是當時經 PR #3／#4 合併掉的）
3. 2 個 fork 也沒動：其中一個的 `pushed_at` 與原 repo **同一秒**（純 fork 未提交），
   另一個停在 2024-10-02

→ **本節所有結論建立在 2024-11-02 那個版本上，而它至今仍是最新版本。**

### 8.2 ⚠️ 它的 README 宣稱了一個沒有實作的功能

README 寫「整合手機感測器（加速度計、陀螺儀）與穿戴裝置（Fitbit、Apple Watch）」。
四條可獨立重跑的證據顯示**程式碼裡沒有任何對應物**：

| # | 查核 | 結果 |
|---|---|---|
| 1 | `pubspec.yaml` 全部相依套件 | 9 個全是 UI 類（fl_chart、lottie、http…），**無 `sensors_plus`、無 `health`** |
| 2 | `grep -ri "accelerom\|sensor\|fitbit\|health" lib/` | **零結果** |
| 3 | `lib/screens/sleep_data/sleep_data_screen.dart:672,681,690` | `hours: "3 hours"` / `"4 hours"` / `"1.5 hours"`，**寫死字串，不發任何 HTTP** |
| 4 | 全 app 呼叫到的睡眠 API | 只有 `/sleep/totalScore` 一個；其餘 11 個端點全屬挑戰與成長系統 |

後端 `business/sleepManager.js` 確實有一個吃加速度計陣列的評分函式，
但**前端沒有任何東西產生那個陣列**。

→ **它是遊戲化 UI 原型，睡眠量測是假的。**

**教訓（寫給我們自己）**：日後評估任何外部專案或函式庫，
**先讀 `pubspec.yaml` / `requirements.txt` 與實際呼叫路徑，再讀 README**。
README 描述的常常是路線圖而不是現況。這跟我們自己的紀律是同一件事——
我們的 `ADVICE_LANG` 設定也曾經寫在文件上卻沒有實作，後來把 `.env.example`
改成誠實說明。

⚠️ **同一份 README 還有第二處與 repo 對不上**：授權（見 8.1 表格與 8.8）。
它寫「採 MIT 授權，詳見 LICENSE 檔」，但那個檔案不存在。
**同一份文件錯兩次，就不是筆誤，是整份不能當事實來源。**

### 8.3 它的評分演算法：五個我們該避免的問題

`business/sleepManager.js` 的四個計分成分：

```js
movementScore   * 0.3   // avgMovement
heartRateScore  * 0.3   // avgHeartRate
remSleepScore   * 0.2   // calculateRemSleep()
lightSleepScore * 0.2   // calculateLightSleep()
```

看起來是四個獨立指標。但往下追這兩個函式：

```js
calculateRemSleep()   → 樣本中 magnitude <  1.5 的比例
calculateLightSleep() → 樣本中 magnitude >= 2.5 的比例
movementScore         → avgMovement
```

| # | 問題 | 位置 | 後果 |
|---|---|---|---|
| 1 | **三個成分是同一個訊號的不同切法** | `sleepManager.js:12,13,22` | 0.3+0.2+0.2 = **70% 的分數只反映加速度 magnitude**。實質是「70% 動作 + 30% 心率」 |
| 2 | **睡眠時長權重為 0** | `sleepManager.js:1` | `duration` 有列在參數，函式體內**一次都沒出現**。文獻支持度最高的核心指標完全不計分 |
| 3 | **心率用固定絕對門檻** | `sleepManager.js:23` | `avgHeartRate < 60 → 100`。靜止心率 72 的人，那 30% **永遠只能拿 50 分**，睡得再好也一樣 |
| 4 | **分數有人為地板** | 所有子分數皆 100/75/50 | 總分範圍被壓在 [50,100]，**糟糕的夜晚彼此無法區分** |
| 5 | **五個門檻零文獻** | `1.5 / 2.5 / 60 / 20 / 50` | 無引用、無單位定義（未扣除重力，靜止時 magnitude 本來就是 1.0 g） |

另外，用加速度大小判定睡眠分期在生理上不成立：**深睡與 REM 都是低動作**，
分不開；而「高動作＝淺睡」其實更接近清醒。

**逐項對照我們的做法**（這些取捨都已經做完，不是待辦）：

| 他們的問題 | 我們對應的既有決定 |
|---|---|
| 三成分同源、重複計分 | 淺層睡眠**不獨立計分**（深睡＋REM＋淺層＝1）；M 節片段化**刻意排除 WASO**，因 Tier1 已佔 25 分 |
| 時長不計分 | 睡眠時長是 Tier1 最高權重 **30 分**（Hirshkowitz 2015） |
| 心率固定門檻 | Tier3 一律用**個人 baseline（近 14–28 晚中位數）**，且只用「當晚之前」的資料避免洩漏 |
| 分數有地板 | `final_score` 為 0–100 連續值，51 晚實測跨越 Good/Normal/Poor/Bad 四級（Good 36／Normal 9／Poor 5／Bad 1）|
| 門檻無文獻 | **每一項計分都有引文**（`Research-Background/Garmin手錶分數.md`）。`MOVEMENT_ACTIVE_THRESHOLD` 是看資料分布訂的，因此**刻意不納入評分** |

### 8.4 ⚠️ 一個我們接下來最容易踩的坑：獎勵與品質脫鉤

這是本節對**未來**最有價值的一條，因為我們還沒做到這一步。

NightBloom 的 app 拿來當遊戲貨幣的 `getTotalScore`（`controllers/sleepController.js:57`）
是把資料庫裡**所有紀錄的分數無條件累加**。配合 8.3 的「分數地板 50」：

> **睡得再差也照領 50 點貨幣。**
> 花園的成長速度只跟「有沒有睡」有關，跟「睡得好不好」幾乎無關。

也就是說，它的遊戲化機制在激勵層面是失效的——玩家最佳策略是「有記錄就好」。

**我們目前沒有這個問題**（`pet_mood` 直接由 `final_quality` 決定，
Bad 的夜晚會真的顯示 `tired` / `anxious`，2026-08-09 那晚就是實例）。
**但一旦加入貨幣、解鎖、寵物成長這類機制，這就是第一個要檢查的地方**：
差的夜晚拿到的回饋，必須明顯少於好的夜晚。

第五節列出的「任務系統、寵物成長、Closet、Rewards」全部落在這個風險區。

> ### ✅ 2026-08-26：任務系統已上線，這條紅線有機械化驗收了
>
> `tests/test_api.py` 有一組專門驗這件事的測試：同一個使用者，
> 一晚準時、一晚遲三小時，**確認遲的那晚拿到的回饋明顯少於準時那晚**。
>
> | | 準時（早 15 分） | 遲三小時 |
> |---|---|---|
> | 寵物心情 | `happy` | `tired` |
> | 「準時放下手機」進度 | **1.0** | **0.0** |
> | 連續紀錄 | 1 晚 | **歸零** |
>
> ⚠️ **另一半的驗收同樣重要，而且更容易被忽略**：
> 「沒測到」的夜晚**不能**被當成失敗，也不能被當成成功。
> `night_achieved()` 回三態（True/False/**None**），
> 沒有資料的窗格回 `insufficient_data` 而不是 0% 進度條——
> 後者會讓沒測到的使用者以為自己表現很差。
>
> ⚠️ **缺資料的夜晚會中斷連續紀錄**，這也是紅線 5 的一部分：
> 若把缺資料當成「跳過不算」，只在自己表現好的日子開 App 的人
> 就能靠**選擇性記錄**累積連續紀錄——獎勵就變成跟「有沒有資料」耦合，
> 正是這一節要防的事。

### 8.5 它比我們強的兩塊（誠實對照）

| | 它有 | 我們的狀況 |
|---|---|---|
| **行為介入層** | `seed/challengesSeed.js` 定義每日挑戰，三種型別：`time`（睡前 1 小時不用螢幕、閱讀 30 分）、`action`（冥想 10 分）、`count`（走 5000 步，帶 `target`/`progress`）。每個挑戰綁獎勵與成就。挑戰頁 30 KB，是全 app 最大的畫面 | **完全沒有**。使用者看完分數與夢境日記就結束了，沒有可以「做」的事 |
| **產品外殼** | 導覽流程（splash→welcome→care→mood diary→relax）、成就頁、花園階段頁、設定、個人檔案、通知設定 | 五頁只有 Home 接了真實資料（見第四節） |

**兩邊的缺口剛好互補**：它是「假量測 + 真迴圈」，我們是「真量測 + 無迴圈」。

### 8.6 由此得出的兩件該做的事

**① 補行為介入層，標的用 SRI（作息規律指數）。**

這條不是抄它，是從我們自己的資料長出來的：

- SRI 已經在 `garmin/apply_recovery_modifier.py` 算好並輸出，但依 6.5
  **刻意不計分、只呈現**——它目前實質上是一筆沒人使用的死資料
- 挑戰的標的應該是**行為**（幾點上床），不是**生理結果**（深睡幾分鐘）。
  使用者控制得了前者，控制不了後者。NightBloom 四個挑戰全是行為，這點它是對的
- **拿 SRI 當挑戰目標，完全繞開了我們不讓它計分的理由**：6.5 的顧慮是
  「不同計算方法算出的 SRI 差異大到不能對照外部常模」，
  而「這 7 天你的上床時間有沒有收斂」是**個人內比較**，不受該問題影響
- 我們有 51 晚實測資料，可以先驗證挑戰難度是否合理再上線

建議形式：「連續 7 天上床時間落在 ±30 分鐘內」→ 寵物解鎖某個狀態。

> ### ⚠️ 2026-08-26 修正：標的**不是** SRI，而且門檻也不是 ±30
>
> 上面那段的推理沒錯，但它是在**「單使用者、有手錶」的前提**下寫的。
> D2 確認要找同學裝 APK 之後，那個前提就不成立了：
>
> **① 標的改用「就寢時間的收斂度」，不是 SRI。**
> SRI 需要 Garmin 的睡眠時間軸，而**D2 的受測者沒有錶**。
> 改用 `lights_out_at`（手機最後互動時間）的收斂程度：
> **同一個構念（作息規律性）、同樣是個人內比較**，因此同樣繞開 6.5 的顧慮；
> 但資料來源換成每個人都有的手機。
> 實作在 `behavior/adherence.py` 的 `bedtime_spread_minutes()`。
> ⚠️ 研究者本人兩者都有 → 那正是 N=1 交叉驗證的標的。
>
> **② ±30 分鐘實測**完全達不到**：36 個 7 天窗格 0/36 = 0%**
> （該使用者的離散度中位數是 103 分鐘）。已改為 **±60 分鐘**（22%）。
> 上面寫「可以先驗證挑戰難度再上線」——**驗了，而且原本的門檻沒過**。
> 這正是那句話存在的價值。
>
> **③ 連續挑戰由 5 晚改為 3 晚。** 第一次校準跑出 0%，一度誤判成
> 挑戰壞掉；查下去發現有一部分是**手錶配戴率造成的假象**
> （51 晚散在 87 個日曆日、**只有 1 段連續 ≥5 天**——最長 13 天，2026-06-03 ~ 06-15；而缺資料會中斷連續紀錄）。
> **那個限制在 Tier A 不存在**，去掉斷點重測後才看得出真實梯度。
>
> ⚠️ 校準樣本 n = 1，門檻是暫定值。重跑指令：
> `python migrate_garmin_to_db.py --simulate-challenges`

**② 把 8.2–8.4 寫進報告的「相關工作」。**

我們「每一項計分都有文獻依據」這件事，需要一個對照組才看得出價值。
8.3 那張表是現成的材料。

### 8.7 ⚠️ 一條新的架構紅線：遊戲化層只讀，不回寫

NightBloom 評分崩壞的根因，是把五個沒有文獻依據的門檻**直接寫進分數**。

我們一直守著相反的紀律（`MOVEMENT_ACTIVE_THRESHOLD` 明明算得出來卻堅持不計分），
但**加遊戲化功能時最容易在這裡破功**——例如「為了讓挑戰有感，給完成挑戰的夜晚
加 5 分」，那一步就會讓分數不再只由文獻決定。

**紅線**：挑戰、獎勵、寵物成長等模組**只讀** `final_score` / `final_quality` / `sri`，
**不得參與** `garmin/evaluate_sleep_quality.py` 與 `apply_recovery_modifier.py`
的任何計算。這條已同步寫進 `CLAUDE.md`。

> ### ✅ 2026-08-26：這條紅線現在是**結構上成立**的，不必再靠紀律守住
>
> 遊戲化完全建立在 **Tier A（手機行為）** 上，而 Tier A 的資料來源是手機，
> 跟評分器根本不在同一條路徑上。`behavior/` 底下**連 Tier B 都不 import**，
> 更不用說評分器——它甚至讀不到 `wearable_nightly`。
>
> 機械化驗收（`tests/test_api.py` 每次都跑）：
>
> ```
> behavior/*.py 的 import 行不得出現
>   evaluate_sleep_quality / apply_recovery_modifier / garmin / wearable
> ```
>
> ⚠️ **一定要比對 import 行，不能單純搜關鍵字**——那樣會抓到規則自己的
> 說明文字（`behavior/__init__.py` 就寫著這條規則），得到一個
> **永遠會失敗的檢查**，而大家很快就會學會忽略它。這個坑實際踩過。
> 測試裡用純 Python 的 `re` 而不是 `subprocess` 呼叫 `grep`，
> 因為 PowerShell 沒有 grep（同型的跨終端機問題本專案也踩過）。
>
> **兩層要合併時（研究者本人同時有 Tier A 與 Tier B）由 API 層負責**，
> 不在 `behavior/` 裡做——否則那個套件就會依賴評分路徑，結構保證就破了。
> 合併規則見 `main.py` 的 `resolve_mood()`：
> **有行為資料時行為優先，生理只做 `anxious` 覆寫**。理由與「挑戰標的
> 必須是行為不是生理結果」完全一樣——使用者準時放下手機卻因為感冒睡不好
> 而看到難過的寵物，就是**因為自己控制不了的事而被懲罰**，
> 那條回饋迴圈一斷，整個計畫書的機制就失效了。

### 8.8 程式碼本身沒有可直接沿用的部分

⚠️ **授權狀態不明，不要直接複製任何程式碼。**
README 寫「本專案採 MIT 授權，詳見 LICENSE 檔」，但 repo 根目錄**沒有 LICENSE 檔**
（2026-08-28 以 GitHub contents API 確認），因此 GitHub 判定為「無授權」。
無授權檔的預設是**保留所有權利**，README 那句話的法律效力是模糊的。

我們目前的用途是**閱讀與對照**，不涉及重製，不受影響。
但**不要照抄程式碼**——何況演算法本來就不值得抄（理由見 8.3）。
資料模型也不要學：`models/SleepData.js` 全部只有 5 個欄位，
**沒有日期、沒有 `user_id`**——所以畫不出趨勢，而
`getSleepDataByUserId` 查詢 `{ userId }` 但 schema 根本沒有這個欄位。
（它的多使用者支援程度跟我們一樣是零，這塊沒得抄，見第七節缺口一。）

真正值得看的是 UI/UX 的組織方式：挑戰卡片把三種型別抽象成
`time` / `action` / `count` 的做法，以及導覽流程的分鏡。

---

## 九、外部開源專案對照之二：Nyxo（2026-08-28）

> **這一節的用途**：第八節的對照組是一個學生專案，可以拿來當「反例」；
> 這一節的對照組是**上架中的商業產品**，性質完全不同——它證明的是
> 「即使做成產品也會犯這些錯」，因此我們的取捨不是學術潔癖。
>
> ⚠️ **分寸要跟第八節反過來拿捏**：NightBloom 是「它做錯了所以我們的原則有價值」，
> Nyxo 是「它在某些地方比我們成熟，但在我們最在意的地方一樣會出錯」。
> 不要把這一節寫成貶低對方——它有七個穿戴裝置來源、四週教練課程、
> 雙語與雲端同步，那些我們都沒有。

### 9.1 對照對象、授權與查核方法

| | |
|---|---|
| 專案 | Nyxo（`hello-nyxo/nyxo-app`），芬蘭商業睡眠 App，App Store／Play 上架中 |
| 規模 | **301 stars**、68 forks、293 個 `.ts/.tsx`、**28040 行** |
| 技術棧 | React Native + AWS Amplify（GraphQL）+ Redux + styled-components + Contentful |
| 定位 | 多來源睡眠追蹤 + 四週睡眠教練課程（付費訂閱，RevenueCat） |
| 授權 | 🔴 **GPL-3.0**。`LICENSE.md` 是 35KB 的 GPLv3 全文，與 GitHub API 的 `license.spdx_id` 一致 |

🔴 **授權結論：一行都不能抄。**
GPL-3.0 是**傳染性**授權——複製任何一段程式碼進 Sonnap，整個 Sonnap 就必須以 GPL 開源。
本節的用途純粹是**閱讀與設計對照**，不涉及重製。

⚠️ 與第八節對照著看，這裡剛好是相反的情形：NightBloom 是「README 宣稱 MIT 但沒有
LICENSE 檔」（授權不明），Nyxo 是「LICENSE 檔實體存在且與 API 一致」（授權明確但更嚴格）。
**兩種情況都不能抄，但理由完全不同**——一個是不知道能不能用，一個是知道用了要付什麼代價。
→ 方法論第 1 點成立：**授權看 LICENSE 檔本身，不看 README、不看直覺。**

**查核方法：`git clone` 下來讀原始碼，不採信 README。** 同第八節。

### 9.1.1 ⚠️ `pushed_at` 陷阱的第二種變形：機器人在推

第八節 8.1.1 的教訓是「看 `pushed_at` 不要看 `updated_at`」。Nyxo 顯示這條規則**還不夠**：

| | |
|---|---|
| `pushed_at` | **2026-04-15T06:47:16Z**（看起來還在活躍開發） |
| master 最後一個功能 commit | **2021-04-24**（`aa3f8d9` "Refactor/upgrade packages"） |

差 **5 年**。`git ls-remote --heads origin` 拉出來的近期分支**全部是 dependabot**
（lodash、handlebars、follow-redirects、picomatch…）加一條 `claude/modernize-react-native-jSSD7`。

→ **`pushed_at` 只證明「有東西被推上去」，不證明「有人在寫產品程式碼」。**
相依套件更新機器人會讓一個停更五年的 repo 看起來每個月都在動。
下次判斷外部專案活躍度，除了 `pushed_at` 還要看**主分支的最後一個功能 commit**。

### 9.2 ⚠️ 這份 repo 不等於那個 App

`hooks/useSleep.tsx:25-27`——整支睡眠 hook 的資料來源被註解掉：

```ts
const windowStart = new Date() //useAppSelector(getGoToSleepWindowStart)
const night = []               //useAppSelector(getNightForSelectedDate)
```

master 停在一次「搬到 Apollo/GraphQL」的重構中途。所以：

- **判斷「Nyxo 這個產品怎麼設計」→ 看 App Store 上的 App 與 README**
- **判斷「這段程式碼寫得對不對」→ 才看 repo**

這跟第八節不一樣：NightBloom 的 repo 就是它的全部，Nyxo 的 repo 是一個時間切片。
9.4 列的缺陷**只能宣稱「這份 2021 年的程式碼有這些問題」**，
不能宣稱「Nyxo 這個 App 有這些問題」。報告若要引用，這句話必須一起寫。

### 9.3 值得參考的四點

**① 多來源 adapter：七個來源 → 一個 `Night` 型別**

`src/helpers/sleep/` 底下七支（`health-kit` / `google-fit` / `oura` / `fitbit` /
`withings` / `polar` / `garmin`），每支只做格式轉換。下游完全不知道資料來自哪支錶。
`Night` 只有 6 個欄位：`sourceId` / `sourceName` / `value`(INBED/ASLEEP/AWAKE) /
`startDate` / `endDate` / `totalDuration`。

→ 印證我們 `wearable/healthconnect_adapter.py` 的方向正確（轉成既有評分器吃的格式、
一個門檻都沒改）。**它的教訓是「介面要窄」**——欄位越少，新增來源越不會污染下游。

⚠️ **但也要看反面**：把 `Night` 壓到只剩三個狀態，代價是**深睡／REM 分期資訊在轉換
時被丟掉**（`garmin-helper.ts:31` 把 `sleepLevelsMap` 的 deep/light/rem 全部標成
`ASLEEP`）。我們 Tier1/2 的分數靠的就是深睡與 REM 比例，**這一點不能學**。
窄介面的代價是資訊損失，要看下游需要什麼。

**② `unfilteredNight` + `mutated`：保留原始量測，允許使用者手動修正**

`typings/Sleepdata.ts` 的 `Day` 同時有 `night` 與 `unfilteredNight`，加一個
`mutated: boolean`。使用者可以改上床／起床時間，但**原始值留著、且標記為已修改**。

→ 我們沒有這一層。`SLEEP_START=01:00` 那 14% 結構上錄不到的夜晚、
TAPO 時間戳壞掉的三天（Issue #19），若有「手動補值 + 標記」的路徑就不必整晚報廢。
而且 `mutated` 讓報告能誠實區分「量到的」與「補的」，比現在直接丟掉更符合
第六節的誠實揭露原則。

**③ 內容走 CMS，不寫死在程式裡**

四週課程的所有文字（課程、章節、專家、習慣範例）放 Contentful，
另有 `contentful-typescript-codegen` 自動生型別。

→ 對我們的意義：**挑戰與寵物的文案不要硬編碼在 Python 裡**。
現在 `behavior/challenges.py` 改一句話就要改程式、重跑 pipeline、重建 APK。
至少該抽成一個 JSON。（不必上 CMS，我們沒有那個規模。）

**④ 它刻意不算 0–100 分——這是一個成熟產品的選擇**

原本預期會找到評分公式，結果 `helpers/rating.ts` 全文只有一個 switch，
把 `ratingValue` 對到 icon／文字／顏色——**而那個值是使用者自己選的**
（bad / ok / good / great，`RatingModal.tsx`）。

**整個 App 沒有任何一行程式計算睡眠品質分數。** 它呈現時長、上床／起床時刻、
效率、趨勢圖、社交時差、加上主觀評分，然後把「怎麼改善」交給教練課程。

→ 這等於用**迴避**的方式繞開了第八節紅線 2（每項計分都要有文獻門檻）。
我們不必跟——我們的 Tier1/2 有 `Research-Background/Garmin手錶分數.md` 撐著，
那正是最難被複製的資產。**但這是報告「相關工作」很好的一句話**：

> 同類商業產品選擇不給出綜合分數，把詮釋權留給使用者與教練；
> 本專案選擇給分，並為每一項計分附上文獻依據。

### 9.4 實測抓到的六項缺陷（2021 版程式碼）

| # | 缺陷 | 位置 | 後果 |
|---|---|---|---|
| 1 | **睡眠效率分子分母顛倒** | `sleep-data-helper.ts:182`，呼叫端 `useSleep.tsx:32` | `totalBedTime / totalSleepTime`。臥床 ≥ 睡著，所以**永遠 ≥ 100%** |
| 2 | **用線性平均算時鐘時間** | `sleep-data-helper.ts:44` `calculateBedtimeWindow()` | 見 9.5 |
| 3 | **死碼** | `sleep-data-helper.ts:129` `calculateSocialJetlag()` | 全 repo 定義了沒人呼叫。社交時差是有文獻的構念，做了卻沒接上 |
| 4 | **Garmin 轉換整支壞掉** | `garmin-helper.ts:11,15,31` | 見下方展開 |
| 5 | **streak 昨天完成今天就歸零** | `helpers/habits.ts:120` | 見 9.7 |
| 6 | **測試蓋在最不需要的地方** | 見下方表 | 1 與 2 全落在沒測試的那一欄 |

**#4 展開**——`garmin-helper.ts` 一支 83 行，四個獨立缺陷：

1. `:11` `new Date(startTimeInSeconds + offset)`——JS 的 `new Date(number)` 吃**毫秒**，
   Garmin 給的是**秒**（型別檔 `Garmin.ts:4` 就叫 `startTimeInSeconds`）。日期會落在 1970 年
2. `:15` `new Date(startDate.valueOf() + durationInSeconds)`——`startDate` 上一行才被
   `.toISOString()` 轉成**字串**，字串 `.valueOf()` 還是字串，字串 + 數字 = 串接 → `Invalid Date`
3. `:31` `Object.values(sleepLevelsMap)` 把 **`awake` 區段也標成 `Value.Asleep`**——清醒被算成睡眠
4. 所有 asleep sample 共用同一個 `id`（用整晚起訖組，不是該區段的）

呼叫端 `store/reducers/sleep.ts:103-104` 更糟：

```ts
const formattedResponse = formatGarminSamples(combinedSleepData) // combinedSleepData 未定義
dispatch(addNights(formatGarminSample()))                        // 少傳參數、且用了單數版
```

組裝 `combinedSleepData` 的迴圈就在上面幾行被註解掉。`formattedResponse` 算完沒人用。
整段包在 `try/catch`，`catch` 只呼叫 `rejectWithValue(undefined)` 也沒 return
→ **功能完全壞掉且靜默，使用者只會看到「沒有資料」**。

✅ **對照 README 要給它一個公道**：README 的功能清單列了 Google Fit / Apple Health /
Oura / Withings / Fitbit / Polar，**沒有列 Garmin**。所以這裡 README 反而比程式碼誠實，
Garmin 是做到一半的分支。**跟 NightBloom「README 吹牛、程式碼是空的」方向相反**，
記錄下來免得把兩者混為一談。

**#6 展開**——測試分布：

| 有測試 | 沒測試 |
|---|---|
| `fitbit-helper.spec.ts`（**404 行**） | `garmin-helper.ts`（缺陷 #4） |
| `oura-helper.spec.ts`（74 行） | `withings-helper.ts`、`google-fit-helper.ts` |
| `polar-helper.spec.ts`（65 行） | **`calculateEfficiency`（缺陷 #1）** |
| `health-kit-helper.spec.ts`（36 行） | **`calculateBedtimeWindow`（缺陷 #2）** |
| `sleep-data-helper.spec.ts`（**15 行，只測 `matchDayAndNight` 一個函式**） | — |

外加一批 component 的 snapshot 測試（`Card`、`SvgIcon`、`IconBold`…）。

模式很清楚：**測試蓋滿了「機械式的格式轉換」，一條都沒蓋到「會算錯的分析邏輯」。**
而且有測試的來源（Oura）程式碼是正確的、沒測試的來源（Garmin）整支是壞的。

→ 這正是 `tests/test_scoring_guards.py` 做對的事——那四條守的都是
**壞掉時不會報錯**的機制。**這份對照可以直接寫進報告當論據。**

### 9.5 🔴 圓形統計：我們自己的實測結果（2026-08-28）

**缺陷 #2 是本節唯一命中我們下一步的東西**，所以實測驗了。

Nyxo 的 `calculateBedtimeWindow()`（`sleep-data-helper.ts:44`）把每晚的上床時刻換算成
「距當日 00:00 幾分鐘」，加總除以天數。問題是**跨午夜的時刻不能用算術平均**：
23:50 與 00:10 只差 20 分鐘，算術平均卻是 720 分 = **中午 12:00**。
它甚至留了一行不知道在解什麼的 hack：`if (averageBedTime > 1440) { averageBedTime = -1440 }`。

我們的曝險面在 `behavior/adherence.py` 的 `bedtime_spread_minutes()`
（挑戰 `consistency` 的標的）。**已驗，結論是我們的實作站得住，不需要修。**

驗法：拿 `garmin_sleep_summary.csv` 的 52 個有效 `sleep_start_time`
（summary 層；評分用的 51 晚是 features 層再過濾一次的結果）跑 46 個滾動 7 晚窗格，
比對三種算法：

| 算法 | 對真正的圓形統計偏離 |
|---|---|
| **Nyxo 的線性平均** | 最大 **998.0** 分鐘、平均 **310.7** 分鐘 |
| **我們現行的作法** | 最大 **8.0** 分鐘、平均 **1.28** 分鐘 |

**對挑戰判定（spread ≤ 60 分鐘）的影響：我們與圓形基準判定不一致的窗格 = 0 個。**
（三種算法通過的都是同 3 個窗格；但這**不代表 Nyxo 的方法可用**——
其餘 43 個窗格它偏離基準最多 998 分鐘，只是那些窗格的離散度本來就遠超門檻，
錯得再多結論也一樣是「不通過」。）

合成案例的對照（`23:50 / 00:10`，實際相差 20 分鐘）：

| | 算出的 spread |
|---|---|
| Nyxo 線性 | **710.0 分鐘** ❌ |
| 我們現行 | 10.0 分鐘 ✅ |
| 圓形基準 | 10.0 分鐘 |

**我們為什麼沒踩到**：`adherence_minutes()` 與 `bedtime_spread_minutes()` 都把差距
正規化到 (−720, +720]，也就是把時鐘當成環狀取較短的一段並保留方向。
`adherence.py` 的 docstring 還記著這個坑是怎麼發現的——
**2026-08-10 評估「就寢時間標準差」方案時，橫跨中午的資料算出過 507 分鐘的假變異度**
（第 6.5 節排除的第二種做法）。那次的教訓直接救了這次。

⚠️ **殘留的 8 分鐘差距是真的，成因也明確**：`bedtime_spread_minutes()` 的 `center`
用的是「展開後偏移的**算術**平均」，不是真正的圓形平均。兩者在資料聚集時幾乎相同，
只有在離散度已經高達數百分鐘時才拉開——而那種窗格早就遠超任何門檻。
**已驗它不受輸入順序影響**（46 個窗格各隨機打亂 40 次，回傳值全部相同）。

⚠️ **這個結論的適用範圍**：校準樣本是同一位使用者的 52 晚，就寢時刻是聚集的。
若 D2 遇到**雙峰作息**的受測者（例如輪班，有些晚上 23:00、有些 11:00），
線性與圓形的中心會拉開更多。但那種人的離散度必然遠超 ±60 分鐘，
**判定結果仍然穩健**，只是回報的數字本身不夠精確。

→ **行動：不改。** 但若之後要把 spread 的數值本身呈現給使用者
（而不只是拿來判定通過與否），就要換成真正的圓形平均。

✅ **SRI 完全不受影響**：`compute_sri()` 是逐分鐘 epoch 比對「同一個時鐘時間，今天與昨天」，
結構上沒有「一天從幾點開始算」這個問題。`apply_recovery_modifier.py:616` 的 docstring
已經寫明這一點，並且明確對比了「就寢時間標準差」那個需要挑錨點的方案。

### 9.6 ⚠️ CI 設定是對的，但沒有人理它

`.github/workflows/test.yml` 在**每次 push** 跑 `yarn lint` 與 `yarn test`，
而 `lint` 的定義是 `eslint ... && tsc --noEmit`——型別檢查**有**納入。

但 9.4 的 `combinedSleepData`（未定義變數）、`formatGarminSample()`（參數數量不對）、
`habits.ts:109` 的 `resultHabits.set(id, ...)`（`id` 這個變數根本不存在），
在 `tsconfig.json` 的 `strict: true` 底下必定是 tsc error。

→ **master 的 CI 是紅的，而且他們照樣合併。**
這比沒有 CI 更糟：`.github` 資料夾的存在會讓後來的人以為這份程式碼被檢查過。

⚠️ **我們有同型風險**：`tests/*.py` 三支都是獨立腳本、沒有 CI，全靠人記得跑。
現行的防線只有 `CLAUDE.md` 驗收指令那一段。**一個沒被執行的檢查，
比沒有檢查更危險，因為它會製造已經檢查過的錯覺。**

### 9.7 ⏭️ streak 邏輯：我們該補的一條測試

`helpers/habits.ts:120`：

```ts
export const shouldResetDayStreak = ({ latestCompletedDate }: Habit): boolean => {
  const today = startOfDay(new Date())
  const lastDate = new Date(latestCompletedDate ?? new Date())
  return !isSameDay(today, lastDate)
}
```

昨天完成 → 今天不是同一天 → 回傳 true → **連續紀錄歸零**，儘管今天根本還沒過完。
正確判準是「距今超過 1 天」。另外 `?? new Date()` 讓「從沒完成過」的習慣
預設成「今天完成過」，方向也是反的。

🔴 **這直接對到我們的挑戰系統。** 「連續 3 晚」是同一個形狀的邏輯，而且我們還多一個
變因：**缺資料的夜晚**（8.4 已經決定「缺資料會中斷連續紀錄」，那是刻意的防選擇性記錄）。

→ **建議補進 `tests/test_scoring_guards.py`**（或 `test_api.py`）三個 case：

| case | 期望 |
|---|---|
| 昨天完成、今天尚未完成 | 連續紀錄**不得**歸零（今天還沒過完） |
| 隔了兩天沒完成 | **必須**歸零 |
| 中間有一晚缺資料 | **必須**歸零（8.4 的既定決定，要鎖住免得日後被「優化」掉） |

第三條特別重要：它是一個**看起來像 bug 的正確行為**，沒有測試鎖住的話，
下一個接手的人很可能會把它「修掉」。

### 9.8 這一節得到的三件事

| # | 事情 | 狀態 |
|---|---|---|
| 1 | 驗證 `bedtime_spread_minutes()` 的圓形統計 | ✅ **已驗，不需修**（見 9.5：46 窗格 0 個判定翻轉） |
| 2 | 給連續挑戰補三條測試 | ⏭️ 待做（見 9.7），十分鐘的事 |
| 3 | 報告「相關工作」加入三方對照 | ⏭️ 待做（見下） |

**第 3 點的具體寫法**——三個專案剛好構成一個光譜：

| | 評分 | 文獻依據 | 對評分機制本身的測試 |
|---|---|---|---|
| NightBloom | ✅ 有 | ❌ 零引用 | ❌ 無 |
| Nyxo | ❌ 刻意不給分 | —（不適用） | ❌ 分析邏輯零測試 |
| **Sonnap** | ✅ 有 | ✅ 每項有引文 | ✅ `test_scoring_guards.py` 4 條 |

這比單獨說「我們有文獻依據」有力得多，因為它同時回答了
「那你怎麼知道那些依據沒有在後續修改中被弄壞」。

**另外一個順手撿到的**：Garmin API 原始資料有 `validation` 欄位
（`MANUAL` / `DEVICE` / …），標示這筆睡眠是裝置量到的還是使用者手動輸入的
（Nyxo 的 `typings/sources/Garmin.ts` 有這個型別）。
**我們的 `garmin_connect_fetch.py` 沒有讀它**（2026-08-28 實測 grep 過，零結果）。
跨 3 名配戴者那 51 晚若混進手動輸入的紀錄，Tier3 baseline 會被污染，目前沒有機制擋。
成本很低、屬於第六節「誠實揭露」的同類改善，優先度由報告時程決定。
