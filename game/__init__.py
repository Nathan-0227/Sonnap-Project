"""
game/ —— 遊戲化層（XP、等級、衣櫃、成就）。

═══════════════════════════════════════════════════════════════════
⚠️ 設計紅線 4：這一層只讀，不得回寫評分層
═══════════════════════════════════════════════════════════════════

這個套件**只能讀** `wearable_nightly.final_quality` 與 `nightly_behavior`
（行為層），**不得 import** `garmin/evaluate_sleep_quality.py`、
`garmin/apply_recovery_modifier.py`，也不得寫入任何分數欄位。

最容易破功的說法是「完成挑戰的夜晚加 5 分」——那一步就毀了「每項計分都有
引文」這個本專案最難複製的資產。要獎勵就在這一層獎勵：給 XP、給衣服，
不動分數。`tests/test_game_rewards.py` 用兩種方式守這條：掃 import，
以及「打過所有遊戲端點之後，評分表與行為表逐欄沒變」。

═══════════════════════════════════════════════════════════════════
⚠️ 設計紅線 5：獎勵要跟品質綁在一起，不能只跟「有資料」綁在一起
═══════════════════════════════════════════════════════════════════

對照組 NightBloom 把分數無條件累加、又有 50 分地板，睡得再差照領 50 點——
成長速度只跟「有沒有睡」有關，遊戲化在激勵上是失效的。

這裡的做法：**熬夜又睡不好的一晚拿 0 XP**。只是「有資料」拿不到任何東西。
而且 XP 的大宗來自**行為**（幾點放下手機，使用者控制得了），生理結果
（睡眠品質）只是較小的加成——理由同 `main.py` 的 `resolve_mood()`：
使用者準時放下手機卻因感冒睡不好，不該因為自己控制不了的事而少拿很多。

═══════════════════════════════════════════════════════════════════
⚠️ 這裡的數字不是「計分」
═══════════════════════════════════════════════════════════════════

XP、等級門檻、衣服解鎖等級都是**遊戲平衡**，不是睡眠評分——它們不進
`final_score`、不進 `total_modifier`、不影響寵物心情或任何挑戰的達成判定，
所以不受紅線 2（每一項計分都要有文獻）約束。同一條界線見
`docs/TAPO_HANDOFF.md` 的「偵測門檻 ≠ 計分門檻」。
"""
