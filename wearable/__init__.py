"""
wearable/ — Tier B：穿戴裝置的客觀睡眠資料

═══════════════════════════════════════════════════════════════════
這一層與 behavior/ 的關係
═══════════════════════════════════════════════════════════════════

    behavior/   Tier A  手機行為（幾點放下手機）  裝置無關  ← 遊戲化建在這裡
    wearable/   Tier B  穿戴裝置生理資料          裝置相依  ← 只做個人內趨勢

兩者刻意分成兩個套件，因為它們的**可比較性完全不同**（見下方）。

═══════════════════════════════════════════════════════════════════
⚠️ 方法學紅線：跨品牌的生理指標不可互相比較
═══════════════════════════════════════════════════════════════════
不同品牌的睡眠分期演算法不互通——**Garmin 的 REM 20% 與 Fitbit 的
REM 20% 不是同一個東西**。

這與本專案已記錄的 Czeisler (2026) SRI 問題**完全同型**
（「光是計算方法不同，就足以改變死亡率、糖尿病、心房顫動模型的
結果與詮釋」），而當初正是因為那個理由決定 SRI 不計分
（PROJECT_STATUS.md 6.5）。同樣的邏輯用在這裡：

    ✅ 個人內比較成立      你這週比上週深睡多
    ❌ 跨使用者排名不成立  你的深睡比小明多

→ **社交功能（好友比較、排行榜）只能建在 Tier A 上。**
  拿跨品牌的深睡比例排名 = 紅線 2 的變體。
  這會改變 Research-Background/社交功能設計.md 功能三的設計。

所以 wearable_nightly 的 source 與 device_brand **不是備註欄位**，
是方法學上的必要資訊——沒有它們就無從判斷兩列數字能不能放在一起看。

═══════════════════════════════════════════════════════════════════
⚠️ 這一層可以 import 評分器，behavior/ 不行
═══════════════════════════════════════════════════════════════════
紅線 8.7 說的是「**遊戲化層**只讀，不得回寫評分層」。
wearable/ 不是遊戲化層，它是評分的**輸入端**——把 Health Connect 的
資料整理成評分器吃得下的格式，然後呼叫**既有的**評分器。

關鍵在於方向：
    wearable/ → 呼叫 evaluate_sleep_quality（單向，且不改任何門檻）
    behavior/ → 完全不碰                      （結構上不可能違反）

⚠️ 絕對不要為 Health Connect 另寫一套評分。一旦有兩套標準，
   「這個分數是怎麼來的」就再也答不清楚，而那是本專案最難複製的資產。
"""
